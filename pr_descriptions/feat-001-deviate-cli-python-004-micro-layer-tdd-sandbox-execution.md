The deviate CLI lacked micro-layer TDD sandbox execution — the lowest-level operational loop where agents write failing tests (RED), implement passing code (GREEN), optionally amend tests (YELLOW), validate compliance (JUDGE), and polish (REFACTOR). This feature delivers the full micro-layer as both manual step-through commands and a fully automated `deviate micro` pipeline, completing the three-layer DeviaTDD architecture (macro scoping, meso engineering, micro execution).

- **Core Agent Infrastructure**: `AgentBackend` with heredoc-pipe subprocess invocation, YAML handover manifest parsing, configurable timeout with 30s-retry-once-backoff, and structured error types (`AgentTimeoutError`, `AgentSubprocessError`, `MalformedHandoverManifestError`). `AgentConfig` Pydantic model with `backend`/`timeout` fields wired into `DeviateConfig`.
- **Tamper Guard**: `git diff --name-only` evaluation on every agent invocation; detects unauthorized modifications to `tests/`, `specs/`, or config files and performs `git restore` rollback. Supports RED-phase test creation and YELLOW-approved amendments as authorized contexts.
- **Slim Auto Prompt Templates**: KV-cacheable static prefix + dynamic task suffix for each phase (`red.md`, `green.md`, `refactor.md`, `judge.md`, `yellow.md`). Constitution and CLAUDE.md governance injected as trailing contextual blocks by the orchestrator.
- **Manual Pre/Post Commands**: Eight phase commands (`red`, `green`, `yellow`, `judge`, `refactor`, `execute`, `e2e`, `hotfix`) each with `pre` (emit JSON contract) and `post` (validate, commit) subcommands. RED rejects passing tests and syntax errors; GREEN runs Tamper Guard and detects YELLOW handover triggers; REFACTOR enforces test invariance with regression rollback.
- **Automated Orchestration**: `deviate micro <TASK_ID>` runs full RED→GREEN→(YELLOW)→JUDGE→REFACTOR→COMPLETED cycle with session state tracking and ledger updates after each phase commit. `--all` processes all PENDING tasks with retry-once-then-abort. `--no-judge` and `--no-refactor` skip gates.
- **Test Suite**: 15 new test modules covering all phases, core infrastructure, and integration paths. All git operations isolated via `tmp_git_repo` fixture. `_run_pytest` mocked to prevent nested subprocess cascade.
