REFACTOR phase upgrade — replace stdlib `ast` with tree-sitter incremental parse. Add dead-code detection (unused functions), duplicated code blocks (similar AST subtrees ≥ 5 lines), cyclomatic complexity warnings (≥ 10). Up to 10x faster re-parses.