# ADHOC_REQUIREMENTS_LEDGER
> Append-only. Managed automatically by /deviate-adhoc. Do not edit manually.

## FR-ADHOC-001: Streaming Pipeline Monitor with Task Status Display

- **Description**: Implement a live-updating task status dashboard that renders during automated `deviate run --all` execution, showing task list with completion markers, a 5-line rolling agent output buffer, and current phase/status indicator.
- **Preconditions**: At least one task exists in `specs/**/tasks.jsonl`. Rich is installed (already a dependency). Terminal is interactive (TTY) or `--json` fallback is active.
- **Inputs/Outputs**: Input: JSONL task ledger, agent subprocess stdout/stderr. Output: Rich `Live` display with formatted task table, agent output buffer, and status bar.

### Acceptance Criteria
1. **AC-ADHOC-001-01**: Given `deviate run --all` with 5 tasks, When tasks are processed sequentially through RED→GREEN→REFACTOR, Then the display shows `[X]` for completed, `[/]` for in-progress, `[ ]` for pending tasks, updated in real-time.
2. **AC-ADHOC-001-02**: Given an agent is executing a phase, When the agent emits stdout/stderr lines, Then the last 5 lines are displayed in a rolling buffer section below the task list, each truncated to one terminal-width line, with newest at the bottom.
3. **AC-ADHOC-001-03**: Given the agent completes a phase transition, When the task record is updated in the ledger, Then the display refreshes the task marker and the status bar reflects the new phase (RED→GREEN→REFACTOR→COMPLETED) within one render cycle.
4. **AC-ADHOC-001-04**: Given stdout is not a TTY (piped or redirected), When `deviate run --all` executes, Then the Live display is disabled and plain-status JSONL events are emitted instead.
5. **AC-ADHOC-001-05**: Given the agent subprocess exits with a non-zero code, When the failure is detected, Then the task marker changes to `[✗]` with the error reason appended, the agent output buffer preserves the failing output, and remaining tasks continue processing.

## FR-ADHOC-003: Meso-Layer Restructuring — Merge Specify into Shard, Introduce Plan Phase

- **Description**: Restructure the DeviaTDD meso-layer workflow by merging the `/deviate-specify` step into `/deviate-shard` and `/deviate-adhoc` (so issues are born as full specs with Gherkin AC, user stories, and edge cases), deprecating `/deviate-specify` as a standalone step, and introducing a new `/deviate-plan` skill that performs per-issue localized research and planning before task decomposition.
- **Preconditions**: Existing DeviaTDD skill infrastructure (`src/deviate/prompts/skills/`), architecture docs (`specs/DeviaTDD-api.md`, `specs/DeviaTDD-architecture.md`), and constitution (`specs/constitution.md`).
- **Inputs/Outputs**: Input: Current skill SKILL.md files, architecture docs, API docs. Output: Updated skill files (shard, adhoc, plan, tasks), updated architecture/API docs, deprecated specify skill.

### Acceptance Criteria
1. **AC-ADHOC-003-01**: Given the `/deviate-shard` skill is invoked, When it produces issue files, Then each issue file contains full spec-level detail including user stories (US-NNN), Gherkin acceptance criteria (Given/When/Then), edge cases, and scope boundaries — not just high-level descriptions.
2. **AC-ADHOC-003-02**: Given the `/deviate-adhoc` skill is invoked, When it produces an issue file, Then the issue file uses the same spec-enriched format as shard (user stories, Gherkin AC, edge cases).
3. **AC-ADHOC-003-03**: Given the `/deviate-specify` skill exists, When a user attempts to invoke it, Then the skill is marked as deprecated and redirects to the new workflow (shard produces specs directly).
4. **AC-ADHOC-003-04**: Given a new `/deviate-plan` skill, When invoked on a specified issue, Then it performs localized codebase research (what changed since epic-level explore), analyzes prior issue implementations, and produces a planning document that contextualizes the issue for current codebase state before `/deviate-tasks` runs.
5. **AC-ADHOC-003-05**: Given the architecture docs (`DeviaTDD-api.md`, `DeviaTDD-architecture.md`), When updated, Then the workflow diagrams reflect the new flow: Macro(Explore→Research→PRD→Shard+Specify) → Meso(Plan→Tasks) → Micro(RED→GREEN→JUDGE→REFACTOR), with HITL Gate 2 moved to after shard.
6. **AC-ADHOC-003-06**: Given the `/deviate-tasks` skill, When it reads an issue for decomposition, Then it consumes the spec-enriched issue format directly (no separate spec.md lookup needed).

## FR-ADHOC-004: DeviaTDD Code Review Skill — /deviate-review with Constitution & PRD Anchoring

- **Description**: Create a `deviate`-integrated `/deviate-review` code review skill that performs structured multi-domain code review over a defined git scope, with mandatory constitution compliance checking and PRD anchoring. The skill follows the DeviaTDD pre/post command pattern and always references either the epic PRD (`specs/{EPIC_SLUG}/prd.md`) or the adhoc PRD (`specs/adhoc/prd.md`).
- **Preconditions**: Git repository with `specs/constitution.md` present. At least one PRD exists (epic or adhoc). `deviate` CLI is installed.
- **Inputs/Outputs**: Input: git diff scope, constitution, PRD. Output: structured review report in markdown (report in `review-report.md`, machine-parseable `Fix Instructions` block).

### Acceptance Criteria
1. **AC-ADHOC-004-01**: Given a git diff scope (working tree or branch), When `deviate review pre` is invoked, Then it emits a JSON contract containing git state, diff files, constitution path, and resolved PRD path (epic first, adhoc fallback).
2. **AC-ADHOC-004-02**: Given a review report is generated with `Fix Instructions`, When `deviate review post` is invoked with the report path, Then it is saved as a timestamped `review-report-{timestamp}.md` under `.deviate/review/reports/`, and the post status is `SUCCESS`. Reports are advisory artifacts — never committed or staged.

## FR-ADHOC-005: Per-Phase Model Configuration with Configurable Model Routing

- **Description**: Introduce a per-phase model configuration layer that allows users to specify which LLM model each DeviaTDD phase uses. The configuration lives in `.deviate/config.toml` under a `[models]` section with a reserved `default` key (applies to all phases) and per-phase override keys. Both `opencode` and `droid` backends receive `--model <id>`. The `claude` backend ignores model config. Phases without an explicit entry fall through to `default`, and when neither exists no `--model` flag is emitted.
- **Preconditions**: `.deviate/config.toml` exists (created by `deviate init`). Both `opencode run --model` and `droid exec --model` flags are validated. Constitution §`Model Tiering` already documents the intended routing.
- **Inputs/Outputs**: Input: `[models]` section in config with optional `default` key and phase-specific keys. Output: model-aware agent invocation that resolves `phase → override → default → none` and passes `--model <id>` when resolved.

### Acceptance Criteria
1. **AC-ADHOC-005-01**: Given `.deviate/config.toml` contains `[models]\ndefault = "opencode/deepseek-v4-flash"`, When any phase invokes the agent, Then the command includes `--model opencode/deepseek-v4-flash`.
2. **AC-ADHOC-005-02**: Given `.deviate/config.toml` has `[models]\ndefault = "opencode/deepseek-v4-flash"\njudge = "opencode/deepseek-v4-pro"`, When the JUDGE phase invokes the agent, Then the command includes `--model opencode/deepseek-v4-pro` (override wins), and when RED invokes, Then the command includes `--model opencode/deepseek-v4-flash` (default applies).
3. **AC-ADHOC-005-03**: Given `.deviate/config.toml` has no `[models]` section, When any phase invokes the agent, Then no `--model` flag is appended.
4. **AC-ADHOC-005-04**: Given `.deviate/config.toml` has `[models]\nplan = "deepseek-v4-pro"`, When the PLAN phase uses the `droid` backend, Then the command includes `["droid", "exec", "--model", "deepseek-v4-pro"]`.
5. **AC-ADHOC-005-05**: Given `.deviate/config.toml` has `[models]\nred = "opencode/deepseek-v4-flash"`, When the RED phase uses the `claude` backend, Then the command is `["claude", "-p"]` without `--model`.
6. **AC-ADHOC-005-06**: Given the constitution documents the default model tiering, When `deviate init` generates `.deviate/config.toml`, Then the `[models]` section is absent.
7. **AC-ADHOC-005-07**: Given a TDD cycle with different models configured per phase, When the cycle runs, Then each phase invocation uses the resolved model for that phase and the handover manifest is parsed consistently.

## FR-ADHOC-006: Offline Context Documentation System — Integrate `context` CLI into DeviaTDD Framework

- **Description**: Integrate the `context` offline documentation CLI into the DeviaTDD framework across all layers — detect the binary at init time, set a config boolean, inject governance mandates into CLAUDE.md/AGENTS.md, and thread `context add` / `context query` instructions through all phase skill prompts so that AI agents use local, version-pinned documentation as their primary reference source instead of web fetching.
- **Preconditions**: `context` binary on `$PATH` (optional — system works without it). `.deviate/config.toml` exists (created by `deviate init`). Existing prompt templates under `src/deviate/prompts/`.
- **Inputs/Outputs**: Input: `context` binary. Output: Updated `DeviateConfig` with `use_context` boolean; updated governance seeds (`claudemd_seed.md`, `agents_seed.md`) with context mandate section; updated `core.md` universal invariants with context reference; updated skill prompts for explore, adhoc, research, plan, and layer preambles with `context add` / `context query` instructions.

### Acceptance Criteria
1. **AC-ADHOC-006-01**: Given the `context` binary is present on `$PATH`, When `deviate init` runs, Then `.deviate/config.toml` contains `use_context = true`.
2. **AC-ADHOC-006-02**: Given the `context` binary is NOT on `$PATH`, When `deviate init` runs, Then `.deviate/config.toml` contains `use_context = false` (or the field is absent, defaulting to false).
3. **AC-ADHOC-006-03**: Given `context` is detected during init, When `deviate init` completes, Then `CLAUDE.md` and `AGENTS.md` both contain a `## Offline Context Documentation System` section with `context query`, `context list`, and `context add` instructions.
4. **AC-ADHOC-006-04**: Given a `/research` phase execution with `context` available, When the architecture subagents perform library-specific analysis, Then the skill prompt instructs using `context query <source> <topic>` as the primary documentation mechanism, with web fetch as last resort.
5. **AC-ADHOC-006-05**: Given a `/plan` phase execution with `context` available, When the planning analyst performs localized codebase research, Then the skill prompt instructs using `context query` for understanding library APIs and framework conventions.
6. **AC-ADHOC-006-06**: Given an `/explore` or `/adhoc` phase execution, When the subagent identifies the project's dependency ecosystem, Then the skill prompt instructs running `context add <source>` for detected frameworks to index their documentation.

## FR-ADHOC-007: Graphite CLI Integration — Optional Branch & PR Management via `gt`

- **Description**: Add an optional `graphite` boolean flag to `deviate init` that persists a `graphite = true` key in `.deviate/config.toml`. When enabled, the system uses Graphite's `gt` CLI for branch creation (replacing `git branch` with `gt create -am`) and PR submission (replacing `gh pr create` with `gt submit --stack`). Governance seeds (`CLAUDE.md`, `AGENTS.md`) include a conditional Graphite workflow section. When false (default), all existing `git` and `gh` behavior is preserved unchanged.
- **Preconditions**: `.deviate/config.toml` exists (created by `deviate init`). `gt` CLI is an optional runtime dependency — system functions without it when `graphite = false`.
- **Inputs/Outputs**: Input: `--graphite` CLI flag on `deviate init`. Output: Updated `DeviateConfig` with `graphite: bool` field; conditional Graphite workflow section in `CLAUDE.md`/`AGENTS.md`; conditional `gt create` in `feature.py:_create_feature_branch`; conditional `gt submit --stack` in `meso.py:_pr_run`; updated PR skill template referencing `gt` as alternative.

### Acceptance Criteria
1. **AC-ADHOC-007-01**: Given `deviate init --graphite` is run, When `.deviate/config.toml` is generated, Then it contains `graphite = true` as a top-level key.
2. **AC-ADHOC-007-02**: Given `deviate init` is run without `--graphite`, When `.deviate/config.toml` is generated, Then it contains `graphite = false` (or the field is absent, defaulting to false).
3. **AC-ADHOC-007-03**: Given `graphite = true` in config, When `deviate init` runs, Then `CLAUDE.md` and `AGENTS.md` contain a `## Graphite Stacked Changes Workflow` section with `gt create -am`, `gt submit --stack`, `gt sync`, and anti-pattern warnings.
4. **AC-ADHOC-007-04**: Given `graphite = false` in config (or absent), When `deviate init` runs, Then `CLAUDE.md` and `AGENTS.md` do NOT contain the Graphite workflow section.
5. **AC-ADHOC-007-05**: Given `graphite = true` in config, When `deviate pr run` executes, Then PR creation uses `gt submit --stack` instead of `gh pr create`, and the subprocess command is `["gt", "submit", "--stack"]`.
6. **AC-ADHOC-007-06**: Given `graphite = true` in config, When `deviate feature create` runs, Then the branch is created via `gt create -am "<message>"` instead of `git branch <name>`.
7. **AC-ADHOC-007-07**: Given `graphite = false` or absent in config, When any command executes, Then all existing `git` and `gh` behavior is preserved — no `gt` commands are invoked.

## FR-ADHOC-008: AST/Structural Analysis Integration — JUDGE + PLAN + REFACTOR (Pareto High-ROI)

- **Description**: Integrate tree-sitter-based AST parsing into the three highest-ROI DeviaTDD phases: JUDGE (structured diff summary replacing raw diff text, ~15x token reduction on Pro tier), PLAN (pre-scan target workstation files, inject file structure appendix, ~50x token reduction on Pro tier), and REFACTOR (upgrade from stdlib `ast` to tree-sitter incremental parse with dead-code/duplication/complexity checks). All other phases are explicitly excluded per token/cost analysis.
- **Preconditions**: `specs/explore/ast-tree-sitter.md` exists with phase-by-phase integration potential table. `specs/constitution.md` defines V4 Pro for JUDGE/PLAN and V4 Flash for REFACTOR. Python 3.13. `tree-sitter>=0.24` is addable as a dependency.
- **Inputs/Outputs**: Input: `git diff` output (JUDGE), issue file with workstation paths (PLAN), modified source files (REFACTOR). Output: structured change summary in judge prompt, file structure appendix in plan prompt, extended REFACTOR checks (dead code, duplication, complexity). New module: `src/deviate/core/treesitter.py`.

### Acceptance Criteria
1. **AC-ADHOC-008-01**: Given a GREEN-phase diff with changed functions, When JUDGE runs `git diff`, Then the judge prompt contains a `## Structured Diff Summary` section listing only changed `FunctionDef`/`ClassDef` nodes with old/new signatures, and raw diff follows below.
2. **AC-ADHOC-008-02**: Given a PLAN phase invoked with target workstation files, When the plan prompt is assembled, Then it contains a `## Target File Structure` appendix with function/class signatures and import blocks extracted via tree-sitter.
3. **AC-ADHOC-008-03**: Given a modified file in REFACTOR, When `_check_return_type_mismatch()` runs, Then it uses tree-sitter incremental parse (re-parsing only changed ranges), detects dead code (unused functions), duplicated code blocks (similar AST subtrees ≥ 5 lines), and cyclomatic complexity ≥ 10.
4. **AC-ADHOC-008-04**: Given AST targets only JUDGE, PLAN, REFACTOR, When RED, GREEN, YELLOW, TASKS, or MACRO execute, Then no tree-sitter code is invoked — excluded phases are unchanged.
5. **AC-ADHOC-008-05**: Given `tree-sitter>=0.24` in `pyproject.toml`, When `from deviate.core.treesitter import ...` executes, Then the module is importable and grammar loads without error.

## FR-ADHOC-009: Pi Agent Backend Integration — First-Class Backend for Micro/Meso Layers

- **Description**: Add the Pi CLI (`@earendil-works/pi-coding-agent`, binary `pi`) as a first-class DeviaTDD backend option for the micro layer (RED/GREEN/YELLOW/JUDGE/REFACTOR/EXECUTE/E2E) and optionally the meso layer (plan/tasks), reusing the existing subprocess + YAML handover manifest contract. Surface Pi-native token observability (`get_session_stats` cache-read ratio) in `.deviate/prompts.log`, and configure Pi to discover DeviaTDD's existing `src/deviate/prompts/skills/*/SKILL.md` catalogue via `~/.pi/agent/skills/` symlinks generated at `deviate init` time — eliminating the need for explicit prompt wrappers per phase.
- **Preconditions**: `specs/explore/pi-agent-backend.md` exists with verified facts: Pi v0.80.2 (MIT), `pi -p` print mode maps 1:1 to `AgentBackend.invoke()` subprocess contract, Agent Skills spec conformance requires `name:` + `description:` YAML frontmatter (already present on all 20 `src/deviate/prompts/skills/*/SKILL.md` files). `BACKEND_COMMANDS` dict and `AgentConfig.backend` Literal at `src/deviate/core/agent.py:60-65` and `src/deviate/state/config.py:12-18` are extension points.
- **Inputs/Outputs**: Input: `.deviate/config.toml` with `[agent].backend = "pi"` and per-phase `[models]` entries. Output: `~/.pi/agent/skills/<skill-name>` symlinks to `src/deviate/prompts/skills/<skill-name>`; `~/.pi/agent/settings.json` with `skillPaths` array pointing to the project skills directory; `prompts.log` enriched with `pi.session_stats` JSON (tokens.input/output/cacheRead/cacheWrite). Pi subprocess: `pi -p` reads prompt from stdin, writes YAML handover manifest to stdout.

### Acceptance Criteria
1. **AC-ADHOC-009-01**: Given `agent.backend = "pi"` in `.deviate/config.toml`, When `deviate` invokes any micro-layer phase, Then the subprocess command is `pi -p` (no `--model` flag for print mode); YAML handover manifest is extracted from stdout via the existing `_YAML_BLOCK_RE` / `_YAML_HANDOVER_MARKER_RE` pipeline.
2. **AC-ADHOC-009-02**: Given the existing `src/deviate/prompts/skills/deviate-red/SKILL.md` (and the other 19 skills) carry `name:` + `description:` YAML frontmatter, When `deviate init` runs with `agent.backend = "pi"`, Then `~/.pi/agent/skills/<skill-name>` symlinks are created (one per skill) pointing back to `src/deviate/prompts/skills/<skill-name>` — Pi discovers skills natively without DeviaTDD rewriting prompts.
3. **AC-ADHOC-009-03**: Given the `[models]` section in `.deviate/config.toml` declares per-phase models (e.g., `red = "anthropic/claude-sonnet-4-5"`), When `deviate init` runs with Pi backend, Then `~/.pi/agent/settings.json` is generated with `provider` and `model` entries — Pi uses the configured model without `--model` CLI flag.
4. **AC-ADHOC-009-04**: Given a Pi subprocess invocation completes, When `.deviate/prompts.log` is written, Then the `AGENT_RESULT` event includes a `pi.session_stats` block with `tokens.input`, `tokens.output`, `tokens.cacheRead`, `tokens.cacheWrite` — exposing cache-hit ratio for token-cost observability.
5. **AC-ADHOC-009-05**: Given `AGENT_CHOICES` is extended with `"pi"`, When `deviate init` runs interactively, Then `--agent pi` is selectable; the `AGENT_TO_BACKEND` map routes `"pi" -> "pi"`.
6. **AC-ADHOC-009-06**: Given Pi has no built-in permission/popup system, When DeviaTDD's micro-sandbox restricts writes to `src/**/*.py`, Then the wrapper-level Tamper Guard still applies — Pi's bash/file tools operate under the same `src/**/*.py` write restriction enforced by DeviaTDD's pre/post-commit hooks; Pi does not bypass the sandbox.
7. **AC-ADHOC-009-07**: Given the `BACKEND_COMMANDS` dict in `src/deviate/core/agent.py:60-65` is extended with `"pi": "pi -p"`, When `AgentBackend.invoke()` resolves `backend = "pi"`, Then the `subprocess.Popen` call spawns `["pi", "-p"]` with `stdin=PIPE`, `stdout=PIPE`, `stderr=PIPE` and routes the prompt via `proc.communicate(input=prompt.encode("utf-8"))` (or streaming equivalent).
8. **AC-ADHOC-009-08**: Given `AgentConfig.backend` Literal in `src/deviate/state/config.py:12-18` is widened to include `"pi"`, When `DeviateConfig(agent={"backend": "pi"})` is constructed, Then Pydantic validation succeeds without `ValidationError`; existing backends (`opencode`, `claude`, `droid`) remain valid.
9. **AC-ADHOC-009-09**: Given the YAML handover manifest extraction pipeline is backend-agnostic, When a Pi agent emits a manifest in the same `<handover_manifest>` tag or fenced YAML block format, Then `AgentBackend.parse_output()` validates it against `HandoverManifest` Pydantic schema with zero backend-specific code paths.
10. **AC-ADHOC-009-10**: Given Pi RPC mode (`pi --mode rpc --no-session`) exposes strict JSONL-over-stdin/stdout with `prompt`/`steer`/`get_session_stats` commands, When DeviaTDD optionally enables RPC mode via `agent.pi_rpc = true` in `.deviate/config.toml`, Then `AgentBackend.invoke()` swaps `_invoke_blocking` for a JSONL-driven RPC client that parses streaming events (`agent_start`, `message_update`, `agent_end`) and emits the handover manifest from the final event — but this is opt-in, print mode remains the default.

## FR-ADHOC-011: Tome Subsystem v1 — Seven Prompt-Only Diátaxis Curation Skills

- **Description**: Ship the v1 Tome Subsystem as seven static `SKILL.md` prompt-only templates under `src/deviate/prompts/skills/tome-*/`, one per component C1-C7 declared in `specs/_product/architecture.md:25-33`. The classifier (C1) gates which of the four quadrant writers (C2-C5) run; the verifier (C6) performs a cross-doc pass after writers complete; the setup skill (C7) bootstraps the Starlight `apps/docs/` site. Each skill carries the canonical YAML frontmatter (`name:`, `description:`, `category:`, `version:`, `aliases:`) established at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11` and uses a new `category: deviatdd-tome-layer` to distinguish the documentation-curation layer from the existing macro/meso/micro/product layers. v1 is purely prompt-only — no Python runtime is added, no `deviate tome <phase>` CLI surface is registered, no `tome/contracts.py` module is created (per `specs/_product/architecture.md:17-21`).

- **Preconditions**: `specs/_product/architecture.md` (219 lines, declares 7 components C1-C7), `specs/_product/flows/flows-tome.md` (278 lines, declares FLOW-04..FLOW-10), `specs/_product/domain-model.md` (84 lines, declares 9 entities), `specs/_product/release-next.md` (52 lines, declares the Tome release goal + 10 ACs). Existing skill infrastructure at `src/deviate/prompts/skills/` (23 skill directories: 20 base + 3 Product-layer added by FR-ADHOC-010). Existing skill installation path `_install_skills_to_agents` at `src/deviate/cli/__init__.py:518-531` is generic and treats all skills uniformly. Constitution at `specs/constitution.md` v0.2.0 is honored (Python 3.13, three-layer architecture, append-only ledger, HITL gates) and requires no bump for this additive layer.

- **Inputs/Outputs**: Input — seed artifacts at `specs/_product/architecture.md`, `specs/_product/flows/flows-tome.md`, `specs/_product/domain-model.md`, `specs/_product/release-next.md`; existing skill template format at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11`; existing reference skills at `src/deviate/prompts/skills/deviate-flows/SKILL.md`, `deviate-architecture/SKILL.md`, `deviate-release/SKILL.md`. Output — seven new directories `src/deviate/prompts/skills/tome-{classify,write-tutorial,write-how-to,write-reference,write-explanation,verify-docs,setup}/`, each containing a `SKILL.md` file with canonical frontmatter and a body that inlines the schemas, action enum, gate behaviors, and quadrant rules declared in `specs/_product/architecture.md` and `specs/_product/flows/flows-tome.md`; one new unit test extending `tests/cli/test_init.py`; symlinks/copies under `.claude/skills/`, `.opencode/skills/`, `.factory/skills/` generated by the existing `_install_skills_to_agents` flow when `deviate setup` runs.

- **User Stories**:
  1. US-011-01: As a developer who just merged a commit that touches a public API, I want `/tome-classify` to read the diff and emit a classification report naming the Diátaxis doc types required so I know which writers to invoke without re-reading the diff myself. *(Ref: FR-ADHOC-011)*
  2. US-011-02: As a developer running `/tome-classify` with `--merge-base`, I want the report to scope to the cumulative change set from main to HEAD so I can prepare docs for the entire branch before opening a PR. *(Ref: FR-ADHOC-011)*
  3. US-011-03: As a developer invoking `/tome-write-tutorial`, `/tome-write-how-to`, `/tome-write-reference`, or `/tome-write-explanation` after the classifier report, I want each writer to be confined to its own quadrant directory (`tutorials/`, `how-to/`, `reference/`, `explanation/`) and reject out-of-quadrant target paths so cross-type contamination cannot enter the docs tree. *(Ref: FR-ADHOC-011)*
  4. US-011-04: As a developer running `/tome-verify-docs` after writers complete, I want a verifier report that surfaces factual inconsistencies, path mistakes, frontmatter schema violations, and cross-type contamination so the failing files can be fixed before merge. *(Ref: FR-ADHOC-011)*
  5. US-011-05: As a developer running `/tome-setup` for the first time in a target repo, I want the Starlight scaffold + four quadrant directories + `content.config.ts` + starter set materialized idempotently so FLOW-04 can resolve target paths on first invocation. *(Ref: FR-ADHOC-011)*
  6. US-011-06: As a DeviaTDD maintainer extending the framework, I want the seven new Tome skills to flow through the existing `_install_skills_to_agents` path without any CLI changes so the change surface stays minimal and the prompt-only constraint (`specs/_product/architecture.md:17`) is upheld. *(Ref: FR-ADHOC-011)*
  7. US-011-07: As a docs consumer browsing the target repo, I want every doc page to carry valid Tome frontmatter (`title`, `description`, `doc_type`, `status`, `last_verified_at`, `verified_sha`, `related_issues`) so I can filter docs by quadrant and check freshness at a glance. *(Ref: FR-ADHOC-011)*

- **Acceptance Criteria**:
  1. **AC-ADHOC-011-01**: Given the canonical skill frontmatter schema at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11`, When the seven new `SKILL.md` files are written under `src/deviate/prompts/skills/tome-{classify,write-tutorial,write-how-to,write-reference,write-explanation,verify-docs,setup}/SKILL.md`, Then each parses as valid YAML frontmatter carrying `name:`, `description:`, `category: deviatdd-tome-layer`, `version: 1.0.0`, and an `aliases:` block containing the kebab-case slash-command (`/tome-classify`, `/tome-write-tutorial`, etc.) plus the `spec:tome-<skill>` invocation form.
  2. **AC-ADHOC-011-02**: Given the C1 input modes declared at `specs/_product/architecture.md:38-42`, When the `tome-classify/SKILL.md` body is read, Then it inlines the four input modes (default HEAD~1, explicit `<sha>`, `--merge-base`, `--working-tree`) and the action enum (`create`, `update`, `no-change`, `human-review`, `setup-required`).
  3. **AC-ADHOC-011-03**: Given the C2-C5 strict quadrant rule at `specs/_product/architecture.md:64`, When any of `tome-write-tutorial`, `tome-write-how-to`, `tome-write-reference`, `tome-write-explanation` is invoked, Then its prompt instructs the agent to reject the write if the target file path is outside the writer's quadrant directory (`tutorials/`, `how-to/`, `reference/`, `explanation/` respectively) and to flag the boundary violation back to FLOW-04 for re-classification.
  4. **AC-ADHOC-011-04**: Given the inlined frontmatter schema at `specs/_product/architecture.md:71-82`, When any writer prompt runs, Then it instructs the agent to emit full markdown with all seven frontmatter fields (`title`, `description`, `doc_type`, `status`, `last_verified_at`, `verified_sha`, `related_issues`) and to use `doc_type: tutorial | how-to | reference | explanation` matching the writer's quadrant.
  5. **AC-ADHOC-011-05**: Given the C6 verifier responsibilities at `specs/_product/architecture.md:32` and `flows-tome.md:201-238`, When `tome-verify-docs/SKILL.md` body is read, Then it inlines the verifier checks (factual consistency, path correctness, no cross-type contamination, valid Starlight location) and the PASS/FAIL/human-review emit format.
  6. **AC-ADHOC-011-06**: Given the C7 idempotent bootstrap contract at `flows-tome.md:240-278`, When `tome-setup/SKILL.md` body is read, Then it instructs the agent to (a) scaffold `apps/docs/` with Starlight, (b) create four quadrant directories under `apps/docs/src/content/docs/`, (c) add `src/content.config.ts` extending `docsSchema()` with Tome frontmatter fields, (d) seed a small starter set (one explanation, one reference, one how-to, one tutorial), and (e) skip-no-op if `apps/docs/` already exists.
  7. **AC-ADHOC-011-07**: Given the v1 prompt-only constraint at `specs/_product/architecture.md:17`, When `git diff src/deviate/` is computed post-implementation, Then no new Python modules are added; no new Typer sub-app is registered; no new phase appears in `_VALID_PHASES` (`src/deviate/state/config.py:21-39`) or `_PHASE_ORDER` (`src/deviate/cli/macro.py:747`); no `tome/contracts.py` exists.
  8. **AC-ADHOC-011-08**: Given the seven skills are part of the existing `_install_skills_to_agents` pipeline, When `discover_skills()` is called post-implementation, Then it returns 30 skill names (23 prior + 7 Tome), each Tome name appears once, and `deviate setup --agent claude` against a fresh workdir installs all seven under `.claude/skills/tome-*/SKILL.md` with byte-equal content to the source templates.
  9. **AC-ADHOC-011-09**: Given the prompt-only constraint and absence of new Python modules, When `mise run test tests/cli/test_init.py -v` runs post-implementation, Then at least one new test (`test_init_creates_tome_skills`) verifies all seven new SKILL.md files exist, parse correctly, and carry the `category: deviatdd-tome-layer` value; `mise run lint` reports zero ruff violations on the new test; full suite completes in < 18s per AGENTS.md performance mandate.
  10. **AC-ADHOC-011-10**: Given `specs/_product/release-next.md` declares 10 acceptance criteria for the Tome release, When this issue is registered, Then the issue's `flow_refs:` frontmatter carries `[FLOW-04, FLOW-05, FLOW-06, FLOW-07, FLOW-08, FLOW-09, FLOW-10]` — covering all seven Tome flows the seven skills serve — and the `ISS-ADH-011` ledger entry mirrors this list in `specs/issues.jsonl`.

## FR-ADHOC-010: Product Layer Skill Scaffolding — `deviate setup` Creates /deviate-flows, /deviate-architecture, /deviate-release

- **Description**: During `deviate setup`, three new Product-layer skill templates (`deviate-flows`, `deviate-architecture`, `deviate-release`) are materialized under `src/deviate/prompts/skills/` so that the existing skill-installation path (`_install_skills_to_agents` → `discover_skills()` → `install_skill()`) exposes them to every configured agent (`claude`, `opencode`, `factory`/`droid`) without any additional CLI wiring. Each skill carries the canonical YAML frontmatter (`name:`, `description:`, `category:`, `version:`, `aliases:`) established at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11` and references the existing seed artifacts at `specs/_product/release-next.md` and `specs/_product/flows/flows-product.md` as its source-of-truth inputs.
- **Preconditions**: `specs/_product/` directory exists with seed content authored (verified at `specs/_product/release-next.md` and `specs/_product/flows/flows-product.md`). `deviate setup` CLI command exists at `src/deviate/cli/__init__.py:555-627` and already invokes `_install_skills_to_agents(workdir, active_agents)` at line 621. Skill template format and frontmatter schema are established at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11` (20 existing skill directories under `src/deviate/prompts/skills/*/`). The existing seed flow document declares the canonical product flows: `FLOW-01 Flows` (driven by `/deviate-flows`), `FLOW-02 Architecture` (driven by `/deviate-architecture`), `FLOW-03 Release` (driven by `/deviate-release`) per `specs/_product/flows/flows-product.md:1-94`.
- **Inputs/Outputs**: Input: existing seed artifacts at `specs/_product/release-next.md` (acceptance criterion: "`deviate setup` will create new /deviate-flows, /deviate-architecture, and /deviate-release skills" at `specs/_product/release-next.md:26`) and `specs/_product/flows/flows-product.md`. Output: three new directories `src/deviate/prompts/skills/deviate-flows/`, `src/deviate/prompts/skills/deviate-architecture/`, `src/deviate/prompts/skills/deviate-release/`, each containing a `SKILL.md` file with canonical frontmatter; symlinks/copies under `.claude/skills/`, `.opencode/skills/`, and `.factory/skills/` generated by the existing `_install_skills_to_agents` flow during `deviate setup`.

### Acceptance Criteria
1. **AC-ADHOC-010-01**: Given `src/deviate/prompts/skills/` contains the canonical 20 skill templates, When the three new Product-layer skill directories are added (`deviate-flows`, `deviate-architecture`, `deviate-release`), Then `discover_skills()` enumerates 23 skills and `_install_skills_to_agents` copies all 23 into each configured agent skills directory (`.claude/skills/`, `.opencode/skills/`, `.factory/skills/`).
2. **AC-ADHOC-010-02**: Given the canonical frontmatter schema at `src/deviate/prompts/skills/deviate-constitution/SKILL.md:1-11`, When each new Product-layer SKILL.md is written, Then it carries the same schema — `name: deviate-<skill>`, `description:` matching the skill purpose, `category: deviatdd-product-layer`, `version: 1.0.0`, and `aliases:` block containing both the kebab-case command (e.g. `/deviate-flows`) and the `spec:<skill>` invocation form.
3. **AC-ADHOC-010-03**: Given `deviate setup` runs against a fresh workdir with `--agent claude`, When the command completes, Then `workdir/.claude/skills/deviate-flows/SKILL.md`, `workdir/.claude/skills/deviate-architecture/SKILL.md`, and `workdir/.claude/skills/deviate-release/SKILL.md` exist with content identical to the source templates under `src/deviate/prompts/skills/` — verifiable by byte-equal `diff`.
4. **AC-ADHOC-010-04**: Given the existing `deviate-flows` skill body, When an agent invokes it via `/deviate-flows`, Then the prompt references `specs/_product/flows/flows-product.md` as the existing seed it must extend (not regenerate) and directs the agent to write new flow files under `specs/_product/flows/` plus update `specs/_product/flows/index.md` (per `specs/_product/flows/flows-product.md:18-32` FLOW-01 Success State).
5. **AC-ADHOC-010-05**: Given the existing `deviate-architecture` skill body, When an agent invokes it via `/deviate-architecture`, Then the prompt directs the agent to produce/maintain `specs/_product/architecture.md` and `specs/_product/domain-model.md`, requires user flows to exist in `specs/_product/flows/` before invocation (per FLOW-02 Preconditions at `specs/_product/flows/flows-product.md:46-47`), and applies a Local/Context-Bridging/Context-Creating classification to the requested change (per FLOW-02 Metrics at `specs/_product/flows/flows-product.md:63`).
6. **AC-ADHOC-010-06**: Given the existing `deviate-release` skill body, When an agent invokes it via `/deviate-release`, Then the prompt requires both architecture and flows to exist as preconditions (per FLOW-03 Preconditions at `specs/_product/flows/flows-product.md:77-79`), accepts a release-goal description from the user, and writes/overrides `specs/_product/release-next.md` — verifiable by reading the file post-invocation.
7. **AC-ADHOC-010-07**: Given `_install_skills_to_agents` performs idempotent installation (existing pattern at `src/deviate/cli/__init__.py:518-531`), When `deviate setup` is re-run on a workdir that already contains `.claude/skills/deviate-flows/`, Then the three Product-layer skills are detected as already-present and skipped (no duplicate copies, no errors).
8. **AC-ADHOC-010-08**: Given the existing skill symlink/copy path covers `claude`, `opencode`, and `factory` agents, When `deviate setup` runs with `--agent opencode` (or any agent whose skills directory the existing code handles), Then the three Product-layer skills appear under `.opencode/skills/deviate-flows/`, `.opencode/skills/deviate-architecture/`, `.opencode/skills/deviate-release/` — same byte-equal content as the source templates.
9. **AC-ADHOC-010-09**: Given the issue scope is intentionally bounded to skill scaffolding (per release acceptance criterion at `specs/_product/release-next.md:26`), When the implementation completes, Then no new Typer sub-app is registered in `src/deviate/cli/__init__.py`, no new phase is added to `_VALID_PHASES` at `src/deviate/state/config.py:21-39`, no new entry appears in `_PHASE_ORDER` at `src/deviate/cli/macro.py:747`, and the constitution at `specs/constitution.md` remains at v0.2.0 (no version bump, no architectural-principle change). The constitution update and Typer wiring are deferred to a follow-up issue per `specs/explore/product-layer.md:154-159`.
10. **AC-ADHOC-010-10**: Given pytest discovers test files via `pyproject.toml:53-54` (`testpaths = ["tests"]`), When the new product-layer skills are added, Then at least one unit test (`tests/cli/test_init.py::test_init_creates_product_layer_skills`) verifies all three new SKILL.md files exist with the expected frontmatter; re-running `mise run test` finishes in < 18s (per AGENTS.md performance mandate); `mise run lint` reports zero ruff violations.


### Scope Extension — Skill Consumer Upgrades (Added post-publication)

> **Note**: This extension was added after the original FR-ADHOC-010 was published. The skill-scaffolding work (ACs 1-10) is the floor; the work in this extension is the ceiling of this issue. No follow-up issue is required for the scope covered here.

- **Extended Description**: In addition to the three new skill templates (`deviate-flows`, `deviate-architecture`, `deviate-release`), this issue also upgrades the two existing DeviaTDD skills that emit issue files — `deviate-shard` (`src/deviate/prompts/skills/deviate-shard/SKILL.md`) and `deviate-adhoc` (`src/deviate/prompts/skills/deviate-adhoc/SKILL.md`) — so they (a) consume the new `_product` specs (`specs/_product/flows/`, `specs/_product/architecture.md`, `specs/_product/release-next.md`) as authoritative context and (b) emit `flow_refs: [FLOW-XX, ...]` in the YAML frontmatter of every issue file they generate. The `deviate adhoc pre` CLI command at `src/deviate/cli/adhoc.py:45-83` gains a `--flow-ref FLOW-01,FLOW-02` Typer option that propagates to the `AdhocRecord.flow_refs` field. The `IssueRecord` Pydantic model in `src/deviate/state/ledger.py` gains an optional `flow_refs: list[str] = Field(default_factory=list)` field; `validate_yaml_frontmatter` at `src/deviate/cli/macro.py:41` recognizes `flow_refs` as a known key; and `_compute_next_issue_id`/`append_issue_record` at `src/deviate/cli/macro.py:177-204, 682-700` thread the field through to `specs/issues.jsonl` round-trip serialization. This is a self-contained scope addition — no follow-up issue needed.
- **Extended Inputs/Outputs**: Input — `--flow-ref FLOW-01,FLOW-02` CLI flag for `deviate adhoc pre`; FR→flow mapping derived from `specs/_product/flows/flows-product.md` during shard execution. Output — `flow_refs:` populated in every emitted issue frontmatter; `IssueRecord.flow_refs` populated in every `specs/issues.jsonl` JSONL line; `AdhocRecord.flow_refs` populated in every `specs/adhoc.jsonl` JSONL line.

### Acceptance Criteria (Extended Scope)
11. **AC-ADHOC-010-11**: Given `specs/_product/flows/flows-product.md` defines `FLOW-01/02/03`, When `deviate shard` runs against a PRD whose FRs map to specific flows, Then the upgraded `deviate-shard` SKILL instructs the agent to read `specs/_product/flows/`, `specs/_product/release-next.md`, `specs/_product/architecture.md`, and `specs/_product/domain-model.md` as authoritative context, and each generated shard frontmatter carries `flow_refs:` populated by the FR→flow mapping (empty list acceptable for zero-flow enabling slices per Vertical Slice Mandate).
12. **AC-ADHOC-010-12**: Given the user invokes `/deviate-adhoc` with a natural-language task that touches Product-layer flows, When the upgraded `deviate-adhoc` skill runs, Then the generated issue frontmatter carries `flow_refs: [FLOW-XX, ...]` inferred from `_product/flows/` and `release-next.md` — overridable via `--flow-ref FLOW-01,FLOW-03` on the `deviate adhoc pre` CLI.
13. **AC-ADHOC-010-13**: Given the existing `AdhocRecord` and `IssueRecord` Pydantic models, When constructed with `flow_refs=["FLOW-01", "FLOW-02"]`, Then Pydantic validation succeeds, `model_dump_json()` includes the field, and round-trip via `model_validate_json(model_dump_json())` preserves the list unchanged.
14. **AC-ADHOC-010-14**: Given `deviate adhoc pre --flow-ref FLOW-01,FLOW-02`, When the command writes to `specs/adhoc.jsonl`, Then the JSONL line contains `"flow_refs": ["FLOW-01", "FLOW-02"]`; malformed values (e.g., `"FLOW-1"`, `"flow-01"`) raise a clear validation error before the ledger write.
15. **AC-ADHOC-010-15**: Given an issue file with `flow_refs: [FLOW-01]` in its frontmatter, When `validate_yaml_frontmatter(shard_content)` runs at `src/deviate/cli/macro.py:659-661`, Then no `[yellow]SHARD_WARNING[/] invalid YAML frontmatter` is emitted and the issue is registered in `specs/issues.jsonl` with the `flow_refs` field preserved.
16. **AC-ADHOC-010-16**: Given a shard's parsed `issue_data` dict at `src/deviate/cli/macro.py:682` contains `"flow_refs": ["FLOW-01", "FLOW-02"]`, When `append_issue_record(ledger_path, issue_data)` is called, Then the appended JSONL line carries the field and reading the ledger back yields the same list — round-trip serialization preserves flow traceability.

## FR-ADHOC-012: TUI Renderer Component — Fixed-Region RPC Streaming Display (C6)

- **Description**: Deliver component C6 of FLOW-04 — a Rich-based TUI renderer at `src/deviate/tui/renderer.py` that renders the last N lines of normalized `AgentEvent` activity into a fixed terminal region and redraws in place on each event, capped at 10 lines max. On phase completion, the live region clears and a final summary persists.
- **Preconditions**: Python 3.13, `rich>=13.0` (already in `pyproject.toml`). Terminal is interactive (TTY) or fallback to final-summary-only. `stdout_lock` at `src/deviate/ui/render.py:24-30` available for thread-safe writes.
- **Inputs/Outputs**: Input: `AgentEvent` instances via `Renderer.feed(event)`. Output: Fixed-region Rich TUI redraw (TTY) or plain-text final summary (non-TTY).
- **Flow Refs**: `FLOW-04`
- **User Stories**:
  1. US-012-01: As a developer running `deviate` with Pi or OMP via RPC, I want agent progress rendered in a compact 10-line TUI region that redraws in place so I can monitor live progress without scrolling. *(Ref: FR-ADHOC-012)*
  2. US-012-02: As a developer on CI or piping output, I want the live TUI region to be skipped automatically so pipelines are not corrupted. *(Ref: FR-ADHOC-012)*
- **Acceptance Criteria**:
  1. AC-ADHOC-012-01: Given a `Renderer(max_lines=10)`, When 15 events are fed sequentially, Then the rendered region contains exactly the last 10 events and does not grow.
  2. AC-ADHOC-012-02: Given a `Renderer` that has received 5 events, When `flush_summary("Final")` is called, Then the live region clears and the final summary prints.
  3. AC-ADHOC-012-03: Given stdout is not a TTY, When `feed(event)` is called, Then no Rich region is rendered.
  4. AC-ADHOC-012-04: Given two threads concurrently calling `feed(event)`, When both execute, Then all writes are serialized through `stdout_lock`.
  5. AC-ADHOC-012-05: Given a `Renderer` instance, When an `AgentEvent` with `kind="unknown"` is fed, Then it renders as `…` without raising.
  6. AC-ADHOC-012-06: Given a `Renderer(max_lines=10)`, When 1,000 simulated events are fed, Then the rendered region never exceeds 10 lines (release-next AC §4 regression).

## FR-ADHOC-013: Flow Ledger as Canonical Source of Truth — `specs/_product/flows.jsonl` + Flow Coverage Report in `explore.md`

- **Description**: Introduce an append-only flow-inventory ledger at `specs/_product/flows.jsonl` (parallel to `specs/issues.jsonl` and `specs/**/tasks.jsonl`) carrying one identity row per `FLOW-XX` and an ordered stream of append-only `FlowEvent` rows (`FLOW_DISCOVERED`, `FLOW_DOCUMENTED`, `FLOW_IMPLEMENTATION_EVIDENCE_ADDED`, `FLOW_CONFIRMED_IMPLEMENTED`, `FLOW_REFERENCED_BY_ISSUE`, `FLOW_INCLUDED_IN_RELEASE`, `FLOW_DEPRECATED`); expose a `deviate explore post` Flow Coverage Report that derives each flow's `discovered_status` / `doc_status` / `impl_status` and a `drift_flag` (one of `PROMPT_ONLY_NO_CODE`, `DOC_ARTIFACT_ONLY`, `DOCUMENTED_BUT_NOT_IMPLEMENTED`, `IMPLEMENTED_BUT_UNDOCUMENTED`, `ORPHANED_FLOW`, `STALE_DRIFT`, `OK`) by replaying ledger events against `specs/_product/flows/index.md` and `specs/issues.jsonl`.
- **Preconditions**: Python 3.13, `pydantic>=2.0`, `pyyaml>=6.0.3`, `rich>=13.0`, `typer>=0.12` (all already declared in `pyproject.toml`). `specs/_product/flows/index.md` exists as the canonical flow-ID inventory. `src/deviate/state/ledger.py` exposes `IssueRecord` / `AdhocRecord` / `TaskRecord` / `RollbackSnapshot` Pydantic models and `_append_with_compound_key` compound-key idempotency. `re.compile(r"^FLOW-\d{2,}$")` validator at `src/deviate/cli/adhoc.py:19` is the canonical flow-ID regex and is reused unchanged.
- **Inputs/Outputs**: Input — opaque emit calls from `deviate explore` / `deviate shard` / `deviate flow` (when added) and the `_flow_id` field on every existing `IssueRecord.flow_refs`. Output — `specs/_product/flows.jsonl` (append-only, one JSON record per line), Flow Coverage table emitted inside `specs/explore/flow-ledger.md` (and any future `specs/explore/<slug>.md`), updated `.gitattributes` rule `specs/_product/flows.jsonl merge=union`, and three new Pydantic models `FlowRecord`, `FlowEvent`, `FlowCoverage` in `src/deviate/state/ledger.py`.
- **Flow Refs**: `FLOW-01`, `FLOW-02`, `FLOW-03`
- **User Stories**:
  1. US-013-01: As a DeviaTDD operator running `deviate explore` against a fresh or existing repo, I want a Flow Coverage Report (`flow_id | actor/job/trigger | documented? | implementation evidence? | last referenced by issue/release? | drift flag`) inserted into `explore.md` so I can spot documented-but-unbuilt, built-but-undocumented, and orphaned flows before shard/architecture/release consume stale state. *(Ref: FR-ADHOC-013)*
  2. US-013-02: As a DeviaTDD maintainer, I want a `specs/_product/flows.jsonl` append-only ledger (parallel to `specs/issues.jsonl`) with `FlowRecord` + `FlowEvent` Pydantic models so that flow identity, documentation events, and implementation-evidence events are event-sourced and replayable. *(Ref: FR-ADHOC-013)*
  3. US-013-03: As a Release Manager verifying the FLOW-04 release-goal acceptance criterion at `specs/_product/release-next.md:58` (`Flow Coverage review dimension surfaces FLOW-04 with full coverage on the epic-tasks ledger entries`), I want `flow_refs: [FLOW-04]` on epic tasks to map to a `flows.jsonl` identity row carrying `FLOW_CONFIRMED_IMPLEMENTED` evidence so the criterion is enforceable at scan time. *(Ref: FR-ADHOC-013)*
  4. US-013-04: As a concurrent contributor working on a feature branch that emits new flow events, I want `specs/_product/flows.jsonl merge=union` in `.gitattributes` so cross-branch merges of append-only rows proceed without conflict markers (parallel to the constitution v0.4.0 rule for `specs/issues.jsonl`). *(Ref: FR-ADHOC-013)*
- **Acceptance Criteria**:
  1. AC-ADHOC-013-01: Given `specs/_product/flows/index.md` enumerates four flows (`FLOW-01..04`), When `deviate explore post` runs against a freshly initialized `flows.jsonl`, Then the file contains one `FlowRecord` row per flow and exactly the canonical `FlowEvent` rows required for status derivation (`FLOW_DISCOVERED` for each flow, `FLOW_DOCUMENTED` for each documented flow, `FLOW_REFERENCED_BY_ISSUE` per `flow_refs` mention in `specs/issues.jsonl`, `FLOW_INCLUDED_IN_RELEASE` for `specs/_product/release-next.md` goal anchor).
  2. AC-ADHOC-013-02: Given `FlowRecord.flow_id = "FLOW-04"` and the corresponding `FlowEvent` stream, When the `FlowCoverage` derivation runs (`derive_flow_coverage(ledger_path) -> list[FlowCoverage]`), Then `discovered_status == "DISCOVERED"`, `doc_status == "DOCUMENTED"`, `impl_status` is derived from the most-recent `FLOW_CONFIRMED_IMPLEMENTED` or `FLOW_IMPLEMENTATION_EVIDENCE_ADDED` event (else `UNCONFIRMED`), and `drift_flag == "DOCUMENTED_BUT_NOT_IMPLEMENTED"` when `doc_status == "DOCUMENTED"` and `impl_status == "UNCONFIRMED"`.
  3. AC-ADHOC-013-03: Given `IssueRecord.flow_refs == ["FLOW-04"]` for any `specs/issues.jsonl` row, When `deviate explore post` replays the ledger, Then `FLOW_REFERENCED_BY_ISSUE` events are appended to `flows.jsonl` with `event_issue_id` matching the source issue ID and `replay == False` is idempotent (re-applying the same `IssueRecord` produces zero new events because of compound-key uniqueness on `(flow_id, event_type, event_issue_id, timestamp)`).
  4. AC-ADHOC-013-04: Given `.gitattributes` declares `specs/_product/flows.jsonl merge=union`, When two concurrent feature branches each append new `FlowEvent` rows and merge, Then git performs a union merge (no conflict markers) and the resulting file contains rows from both branches in original insertion order per branch (matches the `specs/issues.jsonl merge=union` constitution v0.4.0 precedent).
  5. AC-ADHOC-013-05: Given `_FLOW_REF_PATTERN = re.compile(r"^FLOW-\d{2,}$")` at `src/deviate/cli/adhoc.py:19`, When any new code references a flow ID, Then it reuses this regex (no new regex introduced) and Pydantic `FlowRecord.flow_id` carries `pattern=r"^FLOW-\d{2,}$"` validation; malformed values (`"FLOW-1"`, `"flow-01"`) raise `ValidationError` before the ledger write.
  6. AC-ADHOC-013-06: Given `deviate explore post` runs against a populated `flows.jsonl`, When the Flow Coverage Report is rendered, Then a Rich-formatted table is printed to stdout carrying the six report columns and one row per flow ID discovered in `flows.jsonl`; rows where `drift_flag in {"DOCUMENTED_BUT_NOT_IMPLEMENTED", "IMPLEMENTED_BUT_UNDOCUMENTED", "ORPHANED_FLOW", "STALE_DRIFT"}` are highlighted in yellow.
  7. AC-ADHOC-013-07: Given `validate_yaml_frontmatter` at `src/deviate/core/validation.py:104-115` is lenient (accepts any well-formed YAML block; does not enumerate known/unknown keys), When an issue frontmatter declares `flow_refs: [FLOW-04]` and is posted via `deviate shard post`, Then no `[yellow]SHARD_WARNING[/] invalid YAML frontmatter` is emitted and the row's `flow_refs` field is preserved through round-trip serialization.
