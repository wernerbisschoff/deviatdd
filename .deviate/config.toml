profile = "full"  # micro-run default when --profile is omitted: full or fast
timeout_seconds = 1800  # agent spawn + test wall clock (seconds)
claim_remote = true

[agent]
backend = "pi"
transport = "rpc"  # pi/omp only; omit on other backends
