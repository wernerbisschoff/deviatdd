profile = "default"
timeout_seconds = 1800
agent_export_mode = "local"

graphite = false
use_context = true
use_libref = true
[agent]
backend = "pi"
transport = "cli"
pi_rpc = false
timeout = 1800

[models]
default = "grok-4.6"

