<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!--
    <li><a href="#flow-catalog">Flow Catalog</a></li>
    <li><a href="#flow-detail">Flow Detail</a></li>
    <li><a href="#flow-diagram">Flow Diagram</a></li>
    <li><a href="#coverage-map">Coverage Map</a></li>
    -->
  </ol>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>

<section id="flow-catalog">
  <h2>Flow Catalog</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One card per <code class="flow-ref">FLOW-NN</code>. Click jumps to the matching detail block below.
  </div>
  <ul class="flow-catalog">
    <li><em>TODO &mdash; one card per flow</em></li>
  </ul>
</section>

<section id="flow-detail">
  <h2>Flow Detail</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One <code>&lt;article class="flow-card"&gt;</code> per flow. 8 fields, in order: <em>Actor</em> · <em>Domain</em> · <em>Trigger</em> · <em>Preconditions</em> · <em>Happy path</em> · <em>Failure modes</em> · <em>Outcomes</em> · <em>Trace</em>. ≤ 2 sentences per field.
  </div>
  <div class="component-block" aria-label="[TODO] Flow detail cards">
    <span class="component-label">[TODO] Flow Detail</span>
    <ul>
      <li>One <code>&lt;article class="flow-card" id="flow-NN"&gt;</code> per flow.</li>
      <li>Order ID ascending.</li>
      <li>Tag FR trace and AC token explicitly in the <em>Trace</em> field.</li>
    </ul>
  </div>
</section>

<section id="flow-diagram">
  <h2>Flow Diagram</h2>

  <div class="callout callout-note">
    <span class="callout-title">Visual order of operations</span>
    A reader should be able to trace user actions through the system using this
    diagram alone. <strong>One diagram per flow group, not per flow.</strong>
    Group by lifecycle phase: <em>intake</em> &rarr; <em>process</em> &rarr; <em>output</em>.
  </div>

  <div class="diagram-slot" aria-label="[TODO] Flow sequence diagram">
    <span class="label">Sequence Diagram Slot</span>
    Replace this placeholder with an <code>&lt;svg viewBox="..."&gt;</code> block.
    Lifelines = dashed vertical lines, message arrows = horizontal lines with
    arrowheads, actor names = boxed labels at the top. The scaffold is
    offline-first &mdash; no JavaScript runtime, so code-block diagram formats
    render as plain text.
  </div>

  <!-- DIAGRAM COMPONENT -->
  <!--
       Sequence-diagram pattern (lifelines + message arrows):

       <svg viewBox="0 0 720 360" role="img" aria-labelledby="flowseq-title" xmlns="http://www.w3.org/2000/svg">
         <title id="flowseq-title">Flow Sequence</title>
         <defs>
           <marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5"
                   markerWidth="6" markerHeight="6" orient="auto-start-reverse">
             <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
           </marker>
           <style>
             .fs-line { stroke: var(--accent-flow); stroke-width: 1.5; fill: none; color: var(--accent-flow); }
             .fs-lifeline { stroke: var(--ink-5); stroke-width: 1; stroke-dasharray: 4 4; }
             .fs-actor { fill: var(--ink-3); stroke: var(--accent-flow); stroke-width: 1.5; }
             .fs-actor-text { fill: var(--fg-0); font-family: var(--font-body); font-size: 13px; text-anchor: middle; dominant-baseline: middle; }
             .fs-msg { fill: var(--fg-1); font-family: var(--font-body); font-size: 12px; }
             .fs-msg.dashed { stroke: var(--accent-flow); stroke-width: 1.5; stroke-dasharray: 4 4; fill: none; }
           </style>
         </defs>
         <g id="fs-actor-u">
           <rect class="fs-actor" x="40" y="20" width="120" height="40" rx="6"/>
           <text class="fs-actor-text" x="100" y="40">User</text>
           <line class="fs-lifeline" x1="100" y1="60" x2="100" y2="340"/>
         </g>
         <g id="fs-actor-a">
           <rect class="fs-actor" x="240" y="20" width="160" height="40" rx="6"/>
           <text class="fs-actor-text" x="320" y="40">Agent (/deviate-flows)</text>
           <line class="fs-lifeline" x1="320" y1="60" x2="320" y2="340"/>
         </g>
         <g id="fs-actor-s">
           <rect class="fs-actor" x="500" y="20" width="160" height="40" rx="6"/>
           <text class="fs-actor-text" x="580" y="40">Specs</text>
           <line class="fs-lifeline" x1="580" y1="60" x2="580" y2="340"/>
         </g>
         <line class="fs-line" x1="100" y1="100" x2="320" y2="100" marker-end="url(#arrow)"/>
         <text class="fs-msg" x="210" y="92">invokes /deviate-flows</text>
         <line class="fs-line" x1="320" y1="140" x2="100" y2="140" marker-end="url(#arrow)"/>
         <text class="fs-msg" x="210" y="132">clarifying questions</text>
         <line class="fs-line" x1="100" y1="180" x2="320" y2="180" marker-end="url(#arrow)"/>
         <text class="fs-msg" x="210" y="172">answers</text>
         <line class="fs-line" x1="320" y1="220" x2="580" y2="220" marker-end="url(#arrow)"/>
         <text class="fs-msg" x="450" y="212">writes flow + index</text>
         <line class="fs-line" x1="580" y1="260" x2="320" y2="260" marker-end="url(#arrow)"/>
         <text class="fs-msg" x="450" y="252">write confirmation</text>
       </svg>
       <figcaption>Replace actors and messages with the real walkthrough.</figcaption>
  -->


</section>

<section id="coverage-map">
  <h2>Coverage Map</h2>
  <div class="callout callout-note">
    <span class="callout-title">Two axes</span>
    <strong>FR → FLOW</strong> shows which flow realizes each requirement; <strong>FLOW → FR</strong> shows what each flow owns. This answers &ldquo;is this FR covered?&rdquo; in one query.
  </div>

  <h3 id="coverage-fr-to-flow">FR &rarr; Flow</h3>
  <table>
    <thead>
      <tr><th>FR Token</th><th>Implemented By</th><th>Realization Status</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="fr">FR-001-INIT</code></td>
        <td><code class="flow-ref">FLOW-01</code></td>
        <td><span class="state-pill active">Active</span></td>
      </tr>
      <tr>
        <td><code class="fr">FR-002-MACRO</code></td>
        <td><code class="flow-ref">FLOW-02</code></td>
        <td><span class="state-pill active">Active</span></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>

  <h3 id="coverage-flow-to-fr">Flow &rarr; FR + AC</h3>
  <table>
    <thead>
      <tr><th>Flow Token</th><th>Realized FRs</th><th>Covered ACs</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="flow-ref">FLOW-01</code></td>
        <td><code class="fr">FR-001-INIT</code>, <code class="fr">FR-005-SESSION</code></td>
        <td><code class="ac">AC-001-INIT-01</code> &hellip; <code class="ac">AC-001-INIT-04</code></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>

<h3 id="coverage-drift">Drift Signals</h3>
  <div class="callout callout-warn">
    <span class="callout-title">Rule</span>
    A flow in <code><<SOURCE_MD>></code> but not here (or vice versa) means the catalog is wrong. This table is the registry of truth.
  </div>
  <table>
    <thead>
      <tr><th>Flow</th><th>Documented?</th><th>Implementation Evidence</th><th>Last Reference</th><th>Drift Flag</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="flow-ref">FLOW-04</code></td>
        <td>Yes</td>
        <td>Pending (v0.1.0 release-goal)</td>
        <td><code>specs/_product/release-next.md</code></td>
        <td><span class="chip chip-todo">DOCUMENTED_BUT_NOT_IMPLEMENTED</span></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
