<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
<<CSS>>
</style>
</head>
<body>
<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!--
    <li><a href="#flow-catalog">1. Flow Catalog</a></li>
    <li><a href="#flow-detail">2. Flow Detail</a></li>
    <li><a href="#flow-diagram">3. Flow Diagram</a></li>
    <li><a href="#coverage-map">4. Coverage Map</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / 4</span> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
      <span class="dot"></span><span class="dot"></span><span class="dot"></span><span class="dot"></span>
    </span>
  </div>
</aside>

<main class="main">

<header class="spec-meta">
  <span class="phase-tag">Phase: flows</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: flows &middot; <strong>Generated by</strong>: <code>deviate html flows</code></p>
  <p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author the body from <code><<SOURCE_MD>></code>.
    This page is the <em>visual</em> catalog; <code>specs/_product/flows/index.md</code>
    is the <em>grep-able</em> inventory. They MUST stay in sync. Use
    <code class="flow-ref">FLOW-NN</code> tokens so downstream layers can reference flows by ID.
    <span class="chip chip-todo">4 sections to fill</span>
  </p>
</header>

<section id="flow-catalog">
  <h2>1. Flow Catalog <span class="chip chip-todo">grid</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Card grid</span>
    One card per <code class="flow-ref">FLOW-NN</code>. The grid below is the visual catalog &mdash;
    cross-references the markdown table at <code><<SOURCE_MD>></code>. Clicking a card
    jumps to its detail block.
  </div>

  <ul class="flow-catalog">
    <!--
    FLOW CARD COMPONENT &mdash; duplicate per flow.

    <li>
      <a href="#flow-01">
        <span class="flow-id"><code class="flow-ref">FLOW-01</code></span>
        <span class="flow-name">Flows</span>
        <span class="flow-meta">Actor: Developer &middot; Domain: Software Engineering &middot; Status: Active</span>
      </a>
    </li>
    <li>
      <a href="#flow-04">
        <span class="flow-id"><code class="flow-ref">FLOW-04</code></span>
        <span class="flow-name">Live-Stream Agent Progress via RPC</span>
        <span class="flow-meta">Actor: Developer &middot; Domain: Software Engineering &middot; Status: Active</span>
      </a>
    </li>
    -->
    <li><em>TODO &mdash; one card per flow</em></li>
  </ul>
</section>

<section id="flow-detail">
  <h2>2. Flow Detail <span class="chip chip-todo">per-flow card</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">One card per flow</span>
    Each <code>&lt;article class="flow-card"&gt;</code> has 8 fields, in order:
    Actor &middot; Domain &middot; Trigger &middot; Preconditions &middot; Happy path
    &middot; Failure modes &middot; Outcomes &middot; Trace (FR/AC tokens).
    Keep each field under two sentences.
  </div>

  <!--
    FLOW DETAIL COMPONENT &mdash; duplicate per flow.

    <article class="flow-card" id="flow-01">
      <h3><code class="flow-ref">FLOW-01</code>: Flows</h3>

      <div class="callout callout-ready">
        <span class="callout-title">Status</span>
        <span class="state-pill active">Active</span> &middot;
        Driver: <code>/deviate-flows</code>
      </div>

      <dl class="kv">
        <dt>Actor</dt><dd>Developer</dd>
        <dt>Domain</dt><dd>Software Engineering</dd>
        <dt>Trigger</dt><dd>User runs <code>/deviate-flows</code> in their agent of choice.</dd>
        <dt>Preconditions</dt><dd>User has an idea of how the product should be used by customers.</dd>
        <dt>Happy path</dt>
        <dd>
          <ol>
            <li>The user runs <code>/deviate-flows</code>.</li>
            <li>The agent converses with the user to determine core customer flows.</li>
            <li>The agent writes <code>flows.md</code> or <code>flows-&lt;domain&gt;.md</code> under <code>specs/_product/</code>.</li>
            <li>The agent updates <code>specs/flows/index.md</code>.</li>
            <li>Flows are referenced by ID throughout lower layers (no context loss).</li>
          </ol>
        </dd>
        <dt>Failure modes</dt>
        <dd>
          <ul>
            <li>User has no clear customer profile &mdash; agent asks 3 clarifying questions then defers.</li>
            <li>Generated flow duplicates an existing <code>FLOW-NN</code> &mdash; agent merges and supersedes.</li>
          </ul>
        </dd>
        <dt>Outcomes</dt>
        <dd>
          <ul>
            <li>A new <code>flows*.md</code> file in <code>specs/_product/</code>.</li>
            <li>The new flows appear in <code>specs/flows/index.md</code>.</li>
            <li>One commit (after user confirmation).</li>
          </ul>
        </dd>
        <dt>Trace</dt>
        <dd>
          <strong>FR</strong>: <code class="fr">FR-002-MACRO</code> &middot;
          <strong>Components</strong>: C1 (<code>deviate</code> CLI) &middot;
          <strong>AC</strong>: <code class="ac">AC-002-MACRO-04</code>
        </dd>
      </dl>
    </article>
    -->

  <div class="component-block" aria-label="[TODO] Flow detail cards">
    <span class="component-label">[TODO] Flow Detail</span>
    One <code>&lt;article class="flow-card" id="flow-NN"&gt;</code> per flow.
    Order ID ascending. Tag every FR trace and AC token explicitly.
  </div>
</section>

<section id="flow-diagram">
  <h2>3. Flow Diagram <span class="chip chip-draft">visual</span></h2>

  <div class="callout callout-note">
    <span class="callout-title">Visual order of operations</span>
    A reader should be able to trace user actions through the system using this
    diagram alone. <strong>One diagram per flow group, not per flow.</strong>
    Group by lifecycle phase: <em>intake</em> &rarr; <em>process</em> &rarr; <em>output</em>.
  </div>

  <div class="diagram-slot" aria-label="[TODO] Flow sequence diagram">
    <span class="label">Sequence Diagram Slot</span>
    Options:
    <br>&nbsp;&nbsp;&middot; <code>&lt;pre class="mermaid"&gt;sequenceDiagram ...&lt;/pre&gt;</code>
    <br>&nbsp;&nbsp;&middot; inline SVG with arrows
    <br>&nbsp;&nbsp;&middot; numbered boxes in HTML for terminal-render fidelity
  </div>

  <!-- DIAGRAM COMPONENT
       <pre class="mermaid">
       sequenceDiagram
           participant U as User
           participant A as Agent (/deviate-flows)
           participant S as Specs
           U-&gt;&gt;A: invokes /deviate-flows
           A-&gt;&gt;U: clarifying questions
           U--&gt;&gt;A: answers
           A-&gt;&gt;S: writes flows.md + updates index.md
           S--&gt;&gt;A: write confirmation
           A-&gt;&gt;U: asks for commit confirmation
           U--&gt;&gt;A: confirms
           A-&gt;&gt;S: single commit on confirmed
       </pre>
  -->
</section>

<section id="coverage-map">
  <h2>4. Coverage Map <span class="chip chip-draft">bidirectional</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Two axes</span>
    <strong>FR &rarr; FLOW</strong> shows which flow realizes each requirement.
    <strong>FLOW &rarr; FR</strong> shows which requirements each flow owns.
    This is how a reviewer answers &ldquo;is this FR covered?&rdquo; in one query.
  </div>

  <h3 id="coverage-fr-to-flow">4.1 FR &rarr; Flow</h3>
  <table>
    <thead>
      <tr><th>FR Token</th><th>Implemented By</th><th>Realization Status</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="fr">FR-001-INIT</code></td>
        <td><code class="flow-ref">FLOW-01</code></td>
        <td><span class="state-pill active">Active</span></td>
      </tr>
      <tr>
        <td><code class="fr">FR-002-MACRO</code></td>
        <td><code class="flow-ref">FLOW-02</code></td>
        <td><span class="state-pill active">Active</span></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>

  <h3 id="coverage-flow-to-fr">4.2 Flow &rarr; FR + AC</h3>
  <table>
    <thead>
      <tr><th>Flow Token</th><th>Realized FRs</th><th>Covered ACs</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="flow-ref">FLOW-01</code></td>
        <td><code class="fr">FR-001-INIT</code>, <code class="fr">FR-005-SESSION</code></td>
        <td><code class="ac">AC-001-INIT-01</code> &hellip; <code class="ac">AC-001-INIT-04</code></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>

  <h3 id="coverage-drift">4.3 Drift Signals</h3>
  <p>If a flow appears in <code><<SOURCE_MD>></code> but not here, or vice versa,
     the catalog is wrong. Use the table below as the registry of truth.</p>

  <table>
    <thead>
      <tr><th>Flow</th><th>Documented?</th><th>Implementation Evidence</th><th>Last Reference</th><th>Drift Flag</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><code class="flow-ref">FLOW-04</code></td>
        <td>Yes</td>
        <td>Pending (v0.1.0 release-goal)</td>
        <td><code>specs/_product/release-next.md</code></td>
        <td><span class="chip chip-todo">DOCUMENTED_BUT_NOT_IMPLEMENTED</span></td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
