<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!-- Section TOC is auto-rendered below by the agent. Anchor IDs MUST match.
         The agent can hand-curate entries (delete sections that don't apply,
         reorder for narrative, etc.). -->
    <!--
    <li><a href="#recommended-architecture">Recommended Architecture</a></li>
    <li><a href="#options-matrix">Options Matrix</a></li>
    <li><a href="#rejected-options">Rejected Options</a></li>
    <li><a href="#design-trade-offs">Design Trade-Offs</a></li>
    <li><a href="#component-diagram">Component Diagram</a></li>
    <li><a href="#adrs">Architectural Decision Records</a></li>
    <li><a href="#source-registry">Source Registry</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / 7</strong> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span>
    </span>
  </div>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <span class="phase-tag">Phase: architecture</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: architecture &middot; <strong>Generated by</strong>: <code>deviate html architecture</code></p>
  <p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author the body from <code><<SOURCE_MD>></code>.
    Markdown is NOT auto-translated; this HTML is the document of record.
    Replace every <code>&lt;!-- TODO --&gt;</code> marker with real content.
    <span class="chip chip-todo">7 sections to fill</span>
  </p>
</header>

<section id="recommended-architecture">
  <h2>Recommended Architecture <span class="chip chip-draft">draft</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Why this section</span>
    Lead with the chosen architecture in 3&ndash;5 short paragraphs (one per major
    component or layer). Cross-reference <code>FLOW-XX</code> tokens where each
    architectural choice is consumed. Use this section to orient the reader
    before diving into the trade-off matrix.
  </div>

  <!--
    RECOMMENDED-ARCHITECTURE COMPONENT
    -- The agent should write prose here. Prefer short paragraphs.
    -- For block diagrams, see the <component-diagram> section below.
    -- For atomic decisions, see ADRs in <adrs>.
  -->

  <div class="component-block" aria-label="[TODO] Recommended Architecture prose">
    <span class="component-label">[TODO] Recommended Architecture</span>
    Write 3&ndash;5 paragraphs. Each paragraph answers:
      (a) What is this component/layer?
      (b) What does it own?
      (c) Which <code>FLOW-XX</code> depends on it?
  </div>
</section>

<section id="options-matrix">
  <h2>Options Matrix</h2>
  <div class="callout callout-note">
    <span class="callout-title">How to fill</span>
    One row per option that was seriously considered (including the recommended one).
    Score each dimension explicitly so the choice is auditable. Mark the recommended
    option&rsquo;s <strong>Verdict</strong> with <span class="chip chip-ready">recommended</span>.
  </div>

  <table>
    <thead>
      <tr>
        <th>Option</th><th>Complexity</th><th>Testability</th>
        <th>Constitutional Alignment</th><th>Reversibility</th><th>Blast Radius</th><th>Verdict</th>
      </tr>
    </thead>
    <tbody>
      <!-- Replace each placeholder row with a real option. -->
      <!--
      <tr>
        <td><strong>Option A &mdash; recommended</strong><br><em>one-line summary</em></td>
        <td>Low</td>
        <td>High</td>
        <td>Aligned &mdash; no exceptions</td>
        <td>Easy (file-local)</td>
        <td>Small</td>
        <td><span class="chip chip-ready">recommended</span></td>
      </tr>
      <tr>
        <td><strong>Option B</strong><br><em>one-line summary</em></td>
        <td>Medium</td>
        <td>Medium</td>
        <td>One exception to §3</td>
        <td>Hard (cross-module)</td>
        <td>Wide</td>
        <td>Rejected</td>
      </tr>
      -->
      <tr>
        <td><em>TODO &mdash; replace with real options</em></td>
        <td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td>
      </tr>
    </tbody>
  </table>
</section>

<section id="rejected-options">
  <h2>Rejected Options <span class="chip chip-draft">rationale</span></h2>
  <p>One bullet per rejected option. <strong>Two sentences max</strong> per bullet &mdash;
     option name, reason it was rejected, what it lost to.</p>
  <ul>
    <!--
    <li><strong>Option X</strong> &mdash; rejected because <em>reason</em>; lost to <em>Option Y</em> on <em>dimension</em>.</li>
    -->
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="design-trade-offs">
  <h2>Design Trade-Offs</h2>
  <p>One row per decision where two reasonable answers exist. The point is
     to leave a trail of <em>why this side</em> for the next maintainer.</p>

  <table>
    <thead>
      <tr><th>Decision</th><th>Trade-off</th><th>Why This Side</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><strong>Use <em>X</em> not <em>Y</em></strong></td>
        <td>Y would offer <em>benefit</em>, but at the cost of <em>cost</em></td>
        <td>Because <em>evidence</em> &mdash; <code>FLOW-XX</code> requires <em>property</em></td>
      </tr>
      -->
      <tr><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

<section id="component-diagram">
  <h2>Component Diagram</h2>
  <div class="callout callout-ready">
    <span class="callout-title">Why HTML beats markdown here</span>
    Diagrams need to <em>render</em>, not just sit between fences. The terminal can
    render <code>mermaid</code> blocks when the rendering pipeline supports it, or
    the agent can swap in a real SVG. <strong>Empty space below is intentional</strong> &mdash;
    the diagram belongs where the reader expects it.
  </div>

  <div class="diagram-slot" aria-label="[TODO] Component diagram">
    <span class="label">Component Diagram Slot</span>
    Embed ONE of:<br>
    &nbsp;&nbsp;&middot; <code>&lt;pre class="mermaid"&gt; ... &lt;/pre&gt;</code> block<br>
    &nbsp;&nbsp;&middot; inline SVG with arrows for C1 &rarr; C2 &rarr; C3<br>
    &nbsp;&nbsp;&middot; ASCII art in <code>&lt;pre&gt;</code> for terminal-rendered output
  </div>

  <!-- COMPONENT-DIAGRAM COMPONENT -->
  <!--
       Drop a mermaid block here. Example:

       <pre class="mermaid">
       flowchart LR
           C1[deviate CLI] --&gt; C2[Subprocess Adapter]
           C2 --&gt; C3[JSONL Framing]
           C3 --&gt; C4[Command Sender]
           C3 --&gt; C5[Event Adapter]
           C5 --&gt; C6[TUI Renderer]
       </pre>
  -->
</section>

<section id="adrs">
  <h2>Architectural Decision Records <span class="chip chip-draft">context&middot;decision&middot;rationale</span></h2>

  <div class="callout callout-note">
    <span class="callout-title">ADR shape</span>
    One <code>&lt;article class="adr"&gt;</code> per decision. Three required fields:
    <strong>Context</strong> (problem + forces), <strong>Decision</strong> (the choice),
    <strong>Rationale</strong> (why this side, ADR criteria explicit). Optional
    <strong>Consequences</strong>. Tag with the relevant <code>FLOW-XX</code>.
  </div>

  <!--
    ADR COMPONENT &mdash; duplicate per decision
    <article class="adr">
      <h3 id="adr-001-spawn-agent-subprocess">ADR-001: Spawn Agent as Subprocess (not In-Process)</h3>
      <dl class="kv">
        <dt>Context</dt><dd>FLOW-04 needs deviate to drive an agent runtime. Pi and OMP both offer an in-process AgentSession SDK and a subprocess --mode rpc mode.</dd>
        <dt>Decision</dt><dd>Spawn the agent as a subprocess and speak JSONL over stdio.</dd>
        <dt>Rationale</dt><dd>Hard to reverse (transport change rewrites C2/C3/C4), surprising without context (Python devs default to in-process SDKs), real tradeoff vs. isolation.</dd>
        <dt>Consequences</dt><dd>Two new components to maintain (C2 + C3); tests must spawn real subprocesses.</dd>
      </dl>
    </article>
  -->

  <div class="component-block" aria-label="[TODO] Add ADR entries">
    <span class="component-label">[TODO] ADRs</span>
    Add one <code>&lt;article class="adr"&gt;</code> per decision.
    Order by significance, newest on top.
  </div>
</section>

<section id="source-registry">
  <h2>Source Registry</h2>
  <div class="callout callout-note">
    <span class="callout-title">Why a registry</span>
    Every fact in this document that came from somewhere <em>other than the
    author&rsquo;s head</em> gets a citation. The registry is the audit trail.
    One row per source &mdash; name, what it informed, link or file reference.
  </div>

  <table>
    <thead>
      <tr><th>Source</th><th>Informs</th><th>Reference</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td><em>doc name or paper title</em></td>
        <td>Section X &mdash; claim Y</td>
        <td><code>path:line</code> or URL</td>
      </tr>
      -->
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
