<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!-- Section TOC is auto-rendered below by the agent. Anchor IDs MUST match.
         The agent can hand-curate entries (delete sections that don't apply,
         reorder for narrative, etc.). -->
    <!--
    <li><a href="#recommended-architecture">Recommended Architecture</a></li>
    <li><a href="#options-matrix">Options Matrix</a></li>
    <li><a href="#rejected-options">Rejected Options</a></li>
    <li><a href="#design-trade-offs">Design Trade-Offs</a></li>
    <li><a href="#component-diagram">Component Diagram</a></li>
    <li><a href="#adrs">Architectural Decision Records</a></li>
    <li><a href="#source-registry">Source Registry</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / <<SECTION_COUNT>></strong> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
<<TOC_DOTS>>
    </span>
  </div>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <span class="phase-tag">Phase: architecture</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: architecture &middot; <strong>Generated by</strong>: <code>deviate html architecture</code></p>
<p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author from <code><<SOURCE_MD>></code>; markdown is <em>not</em> auto-translated.
    <span class="chip chip-todo"><<SECTION_COUNT>> sections to fill</span>
  </p>

<section id="recommended-architecture">
  <h2>Recommended Architecture <span class="chip chip-draft">draft</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    3&ndash;5 short paragraphs; one per major layer. Tag each with the <code>FLOW-XX</code> that consumes it.
  </div>
  <!-- RECOMMENDED-ARCHITECTURE COMPONENT -->
  <div class="component-block" aria-label="[TODO] Recommended Architecture prose">
    <span class="component-label">[TODO] Recommended Architecture</span>
    <ul>
      <li>What is this layer?</li>
      <li>What does it own?</li>
      <li>Which <code>FLOW-XX</code> depends on it?</li>
    </ul>
  </div>
</section>

<section id="options-matrix">
  <h2>Options Matrix</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One row per seriously-considered option (including recommended). Tag the recommended row with <span class="chip chip-ready">recommended</span>.
  </div>
  <table>
    <thead>
      <tr>
        <th>Option</th><th>Complexity</th><th>Testability</th>
        <th>Constitutional Alignment</th><th>Reversibility</th><th>Blast Radius</th><th>Verdict</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><em>TODO &mdash; replace with real options</em></td>
        <td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td>
      </tr>
    </tbody>
  </table>
</section>

<section id="rejected-options">
  <h2>Rejected Options <span class="chip chip-draft">rationale</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One bullet per rejection. ≤ 2 sentences: option, reason, what it lost to.
  </div>
  <ul>
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="design-trade-offs">
  <h2>Design Trade-Offs</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One row per decision where two reasonable answers exist. Cite the <code>FLOW-XX</code> driving the choice.
  </div>
  <table>
    <thead>
      <tr><th>Decision</th><th>Trade-off</th><th>Why This Side</th></tr>
    </thead>
    <tbody>
      <tr><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

<section id="component-diagram">
  <h2>Component Diagram</h2>
  <div class="callout callout-ready">
    <span class="callout-title">Constraint</span>
Inline <code>&lt;svg&gt;</code> only. The scaffold is offline-first; code-fence diagram formats render as plain text.
  </div>
  <div class="diagram-slot" aria-label="[TODO] Component diagram">
    <span class="label">Component Diagram Slot</span>
    Replace this placeholder with an <code>&lt;svg viewBox="..."&gt;</code> block (≤ 12 nodes).
  </div>
  <!-- COMPONENT-DIAGRAM COMPONENT -->
  <!--
       <svg viewBox="0 0 720 280" role="img" aria-labelledby="cd-title" xmlns="http://www.w3.org/2000/svg">
         <title id="cd-title">Component Diagram</title>
         <defs>
           <marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5"
                   markerWidth="6" markerHeight="6" orient="auto-start-reverse">
             <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
           </marker>
           <style>
             .cd-rect { fill: var(--ink-3); stroke: var(--accent-flow); stroke-width: 1.5; }
             .cd-text { fill: var(--fg-0); font-family: var(--font-body); font-size: 14px; text-anchor: middle; dominant-baseline: middle; }
             .cd-edge { fill: none; stroke: var(--accent-flow); stroke-width: 1.5; color: var(--accent-flow); }
           </style>
         </defs>
         <g id="cd-c1">
           <rect class="cd-rect" x="40" y="100" width="160" height="60" rx="8"/>
           <text class="cd-text" x="120" y="130">C1 — caller</text>
         </g>
         <g id="cd-c2">
           <rect class="cd-rect" x="280" y="100" width="160" height="60" rx="8"/>
           <text class="cd-text" x="360" y="130">C2 — boundary</text>
         </g>
         <g id="cd-c3">
           <rect class="cd-rect" x="520" y="100" width="160" height="60" rx="8"/>
           <text class="cd-text" x="600" y="130">C3 — handler</text>
         </g>
         <line class="cd-edge" x1="200" y1="130" x2="280" y2="130" marker-end="url(#arrow)"/>
         <line class="cd-edge" x1="440" y1="130" x2="520" y2="130" marker-end="url(#arrow)"/>
       </svg>
       <figcaption>Replace the three nodes and two edges with your real components.</figcaption>
  -->
</section>


<section id="adrs">
  <h2>Architectural Decision Records <span class="chip chip-draft">context&middot;decision&middot;rationale</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One <code>&lt;article class="adr"&gt;</code> per decision. Fields: <strong>Context</strong> · <strong>Decision</strong> · <strong>Rationale</strong> (cite <code>FLOW-XX</code>). Optional <strong>Consequences</strong>.
  </div>
  <!--
    ADR COMPONENT &mdash; duplicate per decision.
    <article class="adr">
      <h3 id="adr-001-spawn-agent-subprocess">ADR-001: Spawn Agent as Subprocess</h3>
      <dl class="kv">
        <dt>Context</dt><dd>FLOW-04 needs deviate to drive an agent runtime.</dd>
        <dt>Decision</dt><dd>Spawn the agent as a subprocess and speak JSONL over stdio.</dd>
        <dt>Rationale</dt><dd>Hard to reverse; real tradeoff vs. isolation.</dd>
        <dt>Consequences</dt><dd>Two new components to maintain.</dd>
      </dl>
    </article>
  -->
  <div class="component-block" aria-label="[TODO] Add ADR entries">
    <span class="component-label">[TODO] ADRs</span>
    <ul>
      <li>One <code>&lt;article class="adr"&gt;</code> per decision.</li>
      <li>Order by significance, newest on top.</li>
    </ul>
  </div>
</section>

<section id="source-registry">
  <h2>Source Registry</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One row per external source: name, what it informed, <code>path:line</code> or URL.
  </div>
  <table class="audit-log">
    <thead>
      <tr><th>Source</th><th>Informs</th><th>Reference</th></tr>
    </thead>
    <tbody>
      <tr><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
