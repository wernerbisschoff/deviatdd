<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!--
    <li><a href="#core-value-proposition">Core Value Proposition</a></li>
    <li><a href="#in-scope-boundaries">In-Scope Boundaries</a></li>
    <li><a href="#out-of-scope-boundaries">Out-of-Scope Boundaries</a></li>
    <li><a href="#architectural-constraints">Architectural Constraints</a></li>
    <li><a href="#functional-flow">Functional Flow &amp; Sequence</a></li>
    <li><a href="#functional-requirements">Functional Requirements</a></li>
    <li><a href="#decision-readiness">Decision Readiness</a></li>
    <li><a href="#clarification-log">Clarification Log</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / 8</strong> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span><span class="dot"></span>
    </span>
  </div>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <span class="phase-tag">Phase: prd</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: prd &middot; <strong>Generated by</strong>: <code>deviate html prd</code></p>
  <p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author the body from <code><<SOURCE_MD>></code>.
    Mark every FR with <code class="fr">FR-NNN-{{ID}}</code> and every AC with
    <code class="ac">AC-NNN-{{ID}}-NN</code> &mdash; the tokens anchor the contract across phases.
    <span class="chip chip-todo">8 sections to fill</span>
  </p>
</header>

<section id="core-value-proposition">
  <h2>Core Value Proposition <span class="chip chip-draft">elevator pitch</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape it like this</span>
    One sentence: <em>who</em> gets <em>what value</em> in <em>what situation</em>.
    If you can&rsquo;t write it in 25 words, the PRD is too broad.
  </div>
  <div class="component-block" aria-label="[TODO] Core value proposition">
    <span class="component-label">[TODO] Core Value Proposition</span>
    Write a 1&ndash;2 sentence elevator pitch. Reference which <code>FLOW-XX</code>
    this PRD serves.
  </div>
</section>

<section id="in-scope-boundaries">
  <h2>In-Scope Boundaries</h2>
  <p>What this PRD <strong>will deliver</strong>. One bullet per shippable thing.</p>
  <ul>
    <!--
    <li><span class="chip chip-ready">in</span> Concrete deliverable #1 &mdash; tied to <code class="fr">FR-NNN-{{ID}}</code></li>
    -->
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="out-of-scope-boundaries">
  <h2>Out-of-Scope Boundaries</h2>
  <p>What this PRD <strong>will not deliver</strong>. One bullet per exclusion.</p>
  <div class="callout callout-warn">
    <span class="callout-title">Rule</span>
    Every <em>out-of-scope</em> item is an implicit promise &mdash;
    the agent will not implement it, the reviewer will not request it.
  </div>
  <ul>
    <!--
    <li><span class="chip chip-draft">out</span> What we explicitly defer, and where it goes instead (link the follow-up issue or roadmap item).</li>
    -->
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="architectural-constraints">
  <h2>Architectural Constraints</h2>

  <h3 id="data-models-and-invariants">Data Models &amp; Invariants</h3>
  <div class="callout callout-note">
    <span class="callout-title">What to write</span>
    Each invariant in two forms: a short name and an executable form.
    Example: <em>Idempotency</em> &mdash; <code>append_issue_record(id=X)</code> called
    twice yields one ledger row, never two.
  </div>
  <div class="component-block" aria-label="[TODO] Data models and invariants">
    <span class="component-label">[TODO] Data Models &amp; Invariants</span>
    One row per invariant. Format:
    <br>&nbsp;&nbsp;<strong>Invariant name</strong>: <em>human description</em><br>
    &nbsp;&nbsp;<strong>Executable form</strong>: <em>code or property</em>
  </div>

  <h3 id="performance-and-scalability">Performance / Scalability Thresholds</h3>
  <div class="callout callout-note">
    <span class="callout-title">Quantify, don&rsquo;t describe</span>
    Every threshold has a number (latency, throughput, memory, p99) and an
    enforcement point (which test or which telemetry dashboard).
  </div>
  <dl class="kv">
    <!--
    <dt>Init latency</dt><dd><code>L_max &le; 500ms</code> on a clean repo (enforced by <code>tests/test_init.py</code>)</dd>
    <dt>Per-agent export</dt><dd><code>&le; 200ms</code></dd>
    <dt>Full test suite</dt><dd><code>&lt; 30s</code></dd>
    -->
    <dt><em>TODO threshold</em></dt><dd><em>number + enforcement point</em></dd>
  </dl>

  <h3 id="security-and-compliance">Security &amp; Compliance Invariants</h3>
  <ul>
    <!--
    <li><strong>No secrets in code</strong> &mdash; nothing in <code>src/</code> or committed config may carry a real secret.</li>
    <li><strong>Tamper Guard</strong> &mdash; <code>tests/</code>, <code>specs/</code>, and <code>.deviate/</code> are write-restricted outside authorized phases.</li>
    <li><strong>No eval/exec on user input</strong> &mdash; YAML frontmatter parsing uses safe load; no <code>eval()</code> on agent stdout.</li>
    -->
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="functional-flow">
  <h2>Functional Flow &amp; Sequence</h2>
  <p>How a real user walks through this PRD end-to-end. Reference <code>FLOW-XX</code>
     tokens; pick up sequence details from the Flow Catalog.</p>

  <div class="diagram-slot" aria-label="[TODO] Sequence / flow diagram">
    <span class="label">Sequence Diagram Slot</span>
    <em>Insert a real sequence or flowchart here.</em>
    Author it as inline SVG (lifelines = vertical lines, messages = horizontal
    arrows with arrowheads). The scaffold is offline-first &mdash; no JavaScript
    runtime, so code-block diagram formats render as plain text.
  </div>

  <!-- DIAGRAM COMPONENT -->
  <!--
       Sequence-diagram pattern (lifelines + message arrows):

       <svg viewBox="0 0 720 320" role="img" aria-labelledby="prdseq-title" xmlns="http://www.w3.org/2000/svg">
         <title id="prdseq-title">Functional Flow Sequence</title>
         <defs>
           <marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5"
                   markerWidth="6" markerHeight="6" orient="auto-start-reverse">
             <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
           </marker>
           <style>
             .ps-line { stroke: var(--accent-flow); stroke-width: 1.5; fill: none; color: var(--accent-flow); }
             .ps-lifeline { stroke: var(--ink-5); stroke-width: 1; stroke-dasharray: 4 4; }
             .ps-actor { fill: var(--ink-3); stroke: var(--accent-flow); stroke-width: 1.5; }
             .ps-actor-text { fill: var(--fg-0); font-family: var(--font-body); font-size: 13px; text-anchor: middle; dominant-baseline: middle; }
             .ps-msg { fill: var(--fg-1); font-family: var(--font-body); font-size: 12px; }
           </style>
         </defs>
         <g id="ps-actor-u">
           <rect class="ps-actor" x="40" y="20" width="120" height="40" rx="6"/>
           <text class="ps-actor-text" x="100" y="40">User</text>
           <line class="ps-lifeline" x1="100" y1="60" x2="100" y2="300"/>
         </g>
         <g id="ps-actor-a">
           <rect class="ps-actor" x="240" y="20" width="160" height="40" rx="6"/>
           <text class="ps-actor-text" x="320" y="40">Agent (/deviate-prd)</text>
           <line class="ps-lifeline" x1="320" y1="60" x2="320" y2="300"/>
         </g>
         <g id="ps-actor-s">
           <rect class="ps-actor" x="500" y="20" width="160" height="40" rx="6"/>
           <text class="ps-actor-text" x="580" y="40">Spec Book</text>
           <line class="ps-lifeline" x1="580" y1="60" x2="580" y2="300"/>
         </g>
         <line class="ps-line" x1="100" y1="100" x2="320" y2="100" marker-end="url(#arrow)"/>
         <text class="ps-msg" x="210" y="92">invokes /deviate-prd</text>
         <line class="ps-line" x1="320" y1="160" x2="580" y2="160" marker-end="url(#arrow)"/>
         <text class="ps-msg" x="450" y="152">writes draft PRD</text>
       </svg>
       <figcaption>Replace actors and messages with the real walkthrough.</figcaption>
  -->



  <div class="component-block" aria-label="[TODO] Sequence narrative">
    <span class="component-label">[TODO] Functional Flow Narrative</span>
    3&ndash;6 numbered steps showing what happens when the user triggers this PRD.
    Each step names the actor and the action.
  </div>
</section>

<section id="functional-requirements">
  <h2>Functional Requirements <span class="chip chip-todo">fill every FR</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">FR/AC token contract</span>
    Every FR MUST be tagged <code class="fr">FR-NNN-{{ID}}</code> and every AC
    with <code class="ac">AC-NNN-{{ID}}-NN</code>. Tokens surface in task ledgers,
    tests, and JUDGE compliance checks; a missing or malformed token breaks
    downstream traceability.
  </div>

  <!--
    FR COMPONENT &mdash; duplicate per requirement.

    <article class="fr-card" id="fr-001-init">
      <h3><code class="fr">FR-001-INIT</code>: CLI Initialization &amp; Governance Provisioning</h3>
      <dl class="kv">
        <dt>Description</dt>
        <dd>The <code>deviate init</code> command scaffolds <code>.deviate/</code>, provisions default config + session, and idempotently updates project governance files.</dd>
        <dt>Preconditions</dt>
        <dd>Repo root identified; <code>.deviate/config.toml</code> + <code>specs/constitution.md</code> either absent or valid.</dd>
        <dt>Inputs / Outputs</dt>
        <dd><strong>In</strong>: <code>--generate-constitution</code>, <code>--agent-export-mode</code>. <strong>Out</strong>: <code>.deviate/config.toml</code>, <code>.deviate/session.json</code>, <code>specs/constitution.md</code>, updated <code>CLAUDE.md</code>/<code>AGENTS.md</code>.</dd>
        <dt>State Transition</dt>
        <dd>None (first-run only).</dd>
        <dt>Exception Strategy</dt>
        <dd>Validation errors on malformed config; partial-recovery for interrupted inits.</dd>
      </dl>

      <h4>Acceptance Criteria (Definition of Done)</h4>
      <ol class="ac-list">
        <li id="ac-001-init-01">
          <span class="chip chip-ac">AC-001-INIT-01</span>
          <div class="gherkin">
            <p><span class="gherkin-keyword given">Given</span> A clean repo root with no <code>.deviate/</code> directory</p>
            <p><span class="gherkin-keyword when">When</span> The user executes <code>deviate init</code></p>
            <p><span class="gherkin-keyword then">Then</span> <code>.deviate/config.toml</code> and <code>.deviate/</code> are created with valid TOML matching <code>DeviateConfig</code>.</p>
          </div>
        </li>
        <li id="ac-001-init-02">
          <span class="chip chip-ac">AC-001-INIT-02</span>
          <div class="gherkin">
            <p><span class="gherkin-keyword given">Given</span> <code>specs/constitution.md</code> does not exist</p>
            <p><span class="gherkin-keyword when">When</span> The user executes <code>deviate init</code></p>
            <p><span class="gherkin-keyword then">Then</span> <code>specs/constitution.md</code> is created from the tokenized boilerplate template.</p>
          </div>
        </li>
      </ol>
    </article>
  -->

  <div class="component-block" aria-label="[TODO] Add FR entries">
    <span class="component-label">[TODO] Functional Requirements</span>
    One <code>&lt;article class="fr-card" id="fr-NNN-{{id}}"&gt;</code> per FR.
    Order by priority (P0 first). Each FR carries its own acceptance criteria
    as numbered ACs with Gherkin Given/When/Then styling.
  </div>
</section>

<section id="decision-readiness">
  <h2>Decision Readiness <span class="chip chip-todo">gate check</span></h2>
  <p>PRD closes only when every box below is checkable. The HITL gate refuses
     to release a PRD with unchecked items.</p>

  <ul class="readiness-list">
    <li><input type="checkbox" disabled> Requirements space clear of technical blindspots</li>
    <li><input type="checkbox" disabled> Interface data type contracts completely defined</li>
    <li><input type="checkbox" disabled> Constitutional exceptions isolated and closed (each exception has an ADR anchor)</li>
    <li><input type="checkbox" disabled> Every FR carries at least one AC</li>
    <li><input type="checkbox" disabled> Every AC names its traceability target (<code class="fr">FR-NNN</code>, <code>FLOW-XX</code>, or test path)</li>
  </ul>

  <h3 id="blocking-decisions">Blocking Decisions</h3>
  <div class="callout callout-warn">
    <span class="callout-title">What lands here</span>
    Open questions so important that the PRD cannot ship without an answer.
    Each blocking decision gets: the question, who owes the answer, the deadline
    (or &ldquo;blocks Gate 1&rdquo;), and the downstream consequence of delay.
  </div>
  <div class="component-block" aria-label="[TODO] Blocking decisions">
    <span class="component-label">[TODO] Blocking Decisions</span>
    One <code>&lt;div class="callout callout-warn"&gt;</code> per blocker. Format:
    <br>&nbsp;&nbsp;<strong>Question</strong>: <em>what we need to know</em><br>
    &nbsp;&nbsp;<strong>Owner</strong>: <em>role or person</em><br>
    &nbsp;&nbsp;<strong>Deadline</strong>: <em>date or phase gate</em><br>
    &nbsp;&nbsp;<strong>Cost of delay</strong>: <em>downstream impact</em>
  </div>
</section>

<section id="clarification-log">
  <h2>Clarification Log <span class="chip chip-draft">audit trail</span></h2>
  <p>Append-only. Each row is one question asked and answered during PRD authoring.
     <strong>Do not edit past entries</strong> &mdash; supersede with a new row.</p>

  <table>
    <thead>
      <tr><th>#</th><th>Question</th><th>Answer</th><th>Owner</th><th>Date</th></tr>
    </thead>
    <tbody>
      <!--
      <tr>
        <td>1</td>
        <td><em>what was asked</em></td>
        <td><em>the answer (verbatim)</em></td>
        <td><em>who answered</em></td>
        <td><code>YYYY-MM-DD</code></td>
      </tr>
      -->
      <tr><td>&mdash;</td><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
