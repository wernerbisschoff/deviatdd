<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!--
    <li><a href="#core-value-proposition">Core Value Proposition</a></li>
    <li><a href="#in-scope-boundaries">In-Scope Boundaries</a></li>
    <li><a href="#out-of-scope-boundaries">Out-of-Scope Boundaries</a></li>
    <li><a href="#architectural-constraints">Architectural Constraints</a></li>
    <li><a href="#functional-flow">Functional Flow &amp; Sequence</a></li>
    <li><a href="#functional-requirements">Functional Requirements</a></li>
    <li><a href="#decision-readiness">Decision Readiness</a></li>
    <li><a href="#clarification-log">Clarification Log</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / <<SECTION_COUNT>></strong> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
<<TOC_DOTS>>
    </span>
  </div>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <span class="phase-tag">Phase: prd</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: prd &middot; <strong>Generated by</strong>: <code>deviate html prd</code></p>
<p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author from <code><<SOURCE_MD>></code>; markdown is <em>not</em> auto-translated.
    Tag every FR <code class="fr">FR-{NNN}-{ID}</code> and every AC <code class="ac">AC-{NNN}-{ID}-NN</code>.
    <span class="chip chip-todo"><<SECTION_COUNT>> sections to fill</span>
  </p>

<section id="core-value-proposition">
  <h2>Core Value Proposition <span class="chip chip-draft">elevator pitch</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    1&ndash;2 sentences: <em>who</em> gets <em>what value</em> in <em>what situation</em>. Cite the <code>FLOW-XX</code> served. ≤ 25 words.
  </div>
  <div class="component-block" aria-label="[TODO] Core value proposition">
    <span class="component-label">[TODO] Core Value Proposition</span>
    <ul>
      <li>One sentence; reference which <code>FLOW-XX</code> this PRD serves.</li>
    </ul>
  </div>
</section>

<section id="in-scope-boundaries">
  <h2>In-Scope Boundaries</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    One bullet per shippable deliverable, tied to a <code class="fr">FR-{NNN}-{ID}</code>.
  </div>
  <ul>
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="out-of-scope-boundaries">
  <h2>Out-of-Scope Boundaries</h2>
  <div class="callout callout-warn">
    <span class="callout-title">Rule</span>
    Each <em>out-of-scope</em> item is an implicit promise &mdash; the agent won't implement it, the reviewer won't request it.
  </div>
  <ul>
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="architectural-constraints">
  <h2>Architectural Constraints</h2>

  <h3 id="data-models-and-invariants">Data Models &amp; Invariants</h3>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    Each invariant: a short name + an executable form. Example &mdash; <em>Idempotency</em>: <code>append_issue_record(id=X)</code> called twice yields one row.
  </div>
  <div class="component-block" aria-label="[TODO] Data models and invariants">
    <span class="component-label">[TODO] Data Models &amp; Invariants</span>
    <ul>
      <li><strong>Invariant name</strong>: human description.</li>
      <li><strong>Executable form</strong>: code or property.</li>
    </ul>
  </div>

  <h3 id="performance-and-scalability">Performance / Scalability Thresholds</h3>
  <div class="callout callout-note">
    <span class="callout-title">Constraint</span>
    Every threshold has a number + an enforcement point (test path or telemetry).
  </div>
  <dl class="kv">
    <dt><em>TODO threshold</em></dt><dd><em>number + enforcement point</em></dd>
  </dl>

  <h3 id="security-and-compliance">Security &amp; Compliance Invariants</h3>
  <ul>
    <li><em>TODO</em></li>
  </ul>
  <!-- SOURCE ANCHOR COMPONENT — use beside any externally grounded claim.
       <blockquote>
         <strong>Source</strong> &mdash; <em>Source: <code>path/to/file.py:42-45</code></em><br>
         &ldquo;verbatim quote that grounds the requirement or constraint&rdquo;
       </blockquote>
  -->
</section>

<section id="functional-flow">
  <h2>Functional Flow &amp; Sequence</h2>
  <div class="callout callout-note">
    <span class="callout-title">Shape</span>
    3&ndash;6 numbered steps; each names the actor and the action. Cite <code>FLOW-NN</code>.
  </div>
  <div class="diagram-slot" aria-label="[TODO] Sequence / flow diagram">
    <span class="label">Sequence Diagram Slot</span>
    Replace with an inline <code>&lt;svg&gt;</code> (lifelines + arrowhead markers).
  </div>

  <!-- SEQUENCE DIAGRAM COMPONENT
       The seven core lifelines are three human actors, three system contexts,
       and the database. Delivery is included as an optional eighth boundary
       because product flows often send a confirmation after payment.

       <figure>
         <svg viewBox="0 0 1360 520" role="img" aria-labelledby="prdseq-title" xmlns="http://www.w3.org/2000/svg">
           <title id="prdseq-title">Product-layer functional flow</title>
           <defs>
             <marker id="prd-seq-arrow" viewBox="0 0 10 10" refX="9" refY="5"
                     markerWidth="6" markerHeight="6" orient="auto-start-reverse">
               <path d="M0,0 L10,5 L0,10 z" fill="currentColor"/>
             </marker>
             <style>
               .ps-line { stroke: var(--accent-flow); stroke-width: 1.5; fill: none; color: var(--accent-flow); }
               .ps-lifeline { stroke: var(--ink-5); stroke-width: 1; stroke-dasharray: 4 4; }
               .ps-actor { fill: var(--ink-3); stroke: var(--accent-flow); stroke-width: 1.5; }
               .ps-actor-text { fill: var(--fg-0); font-family: var(--font-body); font-size: 11px; text-anchor: middle; dominant-baseline: middle; }
               .ps-msg { fill: var(--fg-1); font-family: var(--font-body); font-size: 11px; }
             </style>
           </defs>
           <g id="prd-actor-organizer">
             <rect class="ps-actor" x="15" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="85" y="40">Organizer</text>
             <line class="ps-lifeline" x1="85" y1="60" x2="85" y2="500"/>
           </g>
           <g id="prd-actor-attendee">
             <rect class="ps-actor" x="185" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="255" y="40">Attendee</text>
             <line class="ps-lifeline" x1="255" y1="60" x2="255" y2="500"/>
           </g>
           <g id="prd-actor-door-staff">
             <rect class="ps-actor" x="355" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="425" y="40">Door Staff</text>
             <line class="ps-lifeline" x1="425" y1="60" x2="425" y2="500"/>
           </g>
           <g id="prd-actor-phoenix-web">
             <rect class="ps-actor" x="525" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="595" y="40">Phoenix Web</text>
             <line class="ps-lifeline" x1="595" y1="60" x2="595" y2="500"/>
           </g>
           <g id="prd-actor-events">
             <rect class="ps-actor" x="695" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="765" y="40">Events</text>
             <line class="ps-lifeline" x1="765" y1="60" x2="765" y2="500"/>
           </g>
           <g id="prd-actor-pay-peach">
             <rect class="ps-actor" x="865" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="935" y="40">Pay+Peach</text>
             <line class="ps-lifeline" x1="935" y1="60" x2="935" y2="500"/>
           </g>
           <g id="prd-actor-db">
             <rect class="ps-actor" x="1035" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="1105" y="40">DB</text>
             <line class="ps-lifeline" x1="1105" y1="60" x2="1105" y2="500"/>
           </g>
           <g id="prd-actor-delivery">
             <rect class="ps-actor" x="1205" y="20" width="140" height="40" rx="6"/>
             <text class="ps-actor-text" x="1275" y="40">Delivery</text>
             <line class="ps-lifeline" x1="1275" y1="60" x2="1275" y2="500"/>
           </g>
           <line class="ps-line" x1="85" y1="100" x2="595" y2="100" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="340" y="92">FLOW-01 — create event</text>
           <line class="ps-line" x1="595" y1="150" x2="765" y2="150" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="680" y="142">FLOW-02 — validate</text>
           <line class="ps-line" x1="765" y1="200" x2="1105" y2="200" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="935" y="192">FLOW-03 — persist draft</text>
           <line class="ps-line" x1="85" y1="250" x2="595" y2="250" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="340" y="242">FLOW-04 — publish event</text>
           <line class="ps-line" x1="595" y1="300" x2="935" y2="300" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="765" y="292">FLOW-05 — create checkout</text>
           <line class="ps-line" x1="935" y1="350" x2="1275" y2="350" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="1105" y="342">FLOW-06 — confirm payment</text>
           <line class="ps-line" x1="1275" y1="400" x2="255" y2="400" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="765" y="392">FLOW-07 — send ticket</text>
           <line class="ps-line" x1="255" y1="450" x2="425" y2="450" marker-end="url(#prd-seq-arrow)"/>
           <text class="ps-msg" x="340" y="442">FLOW-08 — validate entry</text>
         </svg>
         <figcaption>Replace actors and messages with the real walkthrough; keep each arrow keyed to a <code>FLOW-NN</code>.</figcaption>
       </figure>
  -->

<div class="component-block" aria-label="[TODO] Sequence narrative">
    <span class="component-label">[TODO] Functional Flow Narrative</span>
    3&ndash;6 numbered steps: actor &rarr; action &rarr; artifact.
  </div>
</section>

<section id="functional-requirements">
  <h2>Functional Requirements <span class="chip chip-todo">fill every FR</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Token contract</span>
    Tag every FR <code class="fr">FR-{NNN}-{ID}</code> and every AC <code class="ac">AC-{NNN}-{ID}-NN</code>. Missing tokens break downstream traceability.
  </div>
  <!--
    FR COMPONENT — duplicate per requirement.
    <article class="fr-card" id="fr-001-event">
      <h3><code class="fr">FR-001-EVENT</code>: Event Lifecycle &amp; Registration <span class="chip chip-ready">P0</span></h3>
      <dl class="kv">
        <dt>Description</dt><dd>An organizer can create, validate, publish, and cancel an event; attendees register only while registration is open.</dd>
        <dt>Preconditions</dt><dd>The organizer is authenticated; the event has a valid venue, schedule, and registration window.</dd>
        <dt>Inputs / Outputs</dt><dd><strong>In</strong>: event details, capacity, registration window. <strong>Out</strong>: an event record, a public registration URL, audit event keyed by <code class="flow-ref">FLOW-01</code>.</dd>
        <dt>State Transition</dt><dd><span class="state-pill initial">EVENT_DRAFT</span> &rarr; <span class="state-pill active">EVENT_VALIDATED</span> &rarr; <span class="state-pill terminal">EVENT_PUBLISHED</span></dd>
        <dt>Exception Strategy</dt><dd>Reject invalid capacity or overlapping schedules without writing a partial record; payment or delivery failures remain retryable.</dd>
      </dl>
      <h4>Acceptance Criteria (Definition of Done)</h4>
      <ol class="ac-list">
        <li id="ac-001-event-01">
          <span class="chip chip-ac">AC-001-EVENT-01</span>
          <div>
            <strong>Observable outcome</strong>: Publishing a validated event exposes its registration URL and records the <span class="state-pill active">EVENT_PUBLISHED</span> state.
            <p><strong>Boundary category</strong>: A duplicate publish request is idempotent and does not create a second event.</p>
          </div>
        </li>
        <li id="ac-001-event-02">
          <span class="chip chip-ac">AC-001-EVENT-02</span>
          <div>
            <strong>Observable outcome</strong>: An attendee receives a confirmation only after capacity and payment checks succeed.
            <p><strong>Error category</strong>: A failed payment leaves the event unchanged and exposes a retryable failure to the caller.</p>
          </div>
        </li>
      </ol>
    </article>
  -->
  <div class="component-block" aria-label="[TODO] Add FR entries">
    <span class="component-label">[TODO] Functional Requirements</span>
    <ul>
      <li>One <code>&lt;article class="fr-card" id="fr-NNN-ID"&gt;</code> per FR.</li>
      <li>Order by priority (P0 first).</li>
      <li>Detailed Given/When/Then scenarios belong in the plan contract, not here.</li>
    </ul>
  </div>
</section>

<section id="decision-readiness">
  <h2>Decision Readiness <span class="chip chip-todo">gate check</span></h2>
  <ul class="readiness-list">
    <li><input type="checkbox" disabled> Requirements space clear of technical blindspots</li>
    <li><input type="checkbox" disabled> Interface data type contracts completely defined</li>
    <li><input type="checkbox" disabled> Constitutional exceptions isolated and closed (each has an ADR anchor)</li>
    <li><input type="checkbox" disabled> Every FR carries at least one AC</li>
    <li><input type="checkbox" disabled> Every AC names its traceability target (<code class="fr">FR-NNN</code>, <code>FLOW-XX</code>, or test path)</li>
  </ul>

  <h3 id="blocking-decisions">Blocking Decisions</h3>
  <div class="callout callout-warn">
    <span class="callout-title">Shape</span>
    One <code>callout-warn</code> per blocker: <strong>Question</strong> · <strong>Owner</strong> · <strong>Deadline</strong> · <strong>Cost of delay</strong>. Gate 1 blocks if any remain open.
  </div>
  <div class="component-block" aria-label="[TODO] Blocking decisions">
    <span class="component-label">[TODO] Blocking Decisions</span>
    <ul>
      <li>One blocker per callout-warn block.</li>
      <li>Owner + deadline + downstream cost &mdash; every field.</li>
    </ul>
  </div>
</section>

<section id="clarification-log">
  <h2>Clarification Log <span class="chip chip-draft">audit trail</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Rule</span>
    Append-only. <strong>Do not edit past entries</strong> &mdash; supersede with a new row.
  </div>
  <table class="audit-log">
    <thead>
      <tr><th>#</th><th>Question</th><th>Answer</th><th>Owner</th><th>Date</th></tr>
    </thead>
    <tbody>
      <tr><td>&mdash;</td><td><em>TODO</em></td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
