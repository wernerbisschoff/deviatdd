<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title><<TITLE>></title>
<meta name="generator" content="deviate html scaffold">
<meta name="source-md" content="<<SOURCE_MD>>">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="dark">
<style>
<<CSS>>
</style>
</head>
<body>
<a class="skip-link" href="#main">Skip to content</a>
<div class="reading-progress" aria-hidden="true"></div>

<div class="layout">

<aside class="sidebar" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol class="toc" id="toc">
    <!--
    <li><a href="#plan-summary">Plan Summary</a></li>
    <li><a href="#acceptance-contract">Acceptance Contract</a></li>
    <li><a href="#workstation-mapping">Workstation Mapping</a></li>
    <li><a href="#implementation-strategy">Implementation Strategy</a></li>
    <li><a href="#data-flow-analysis">Data Flow Analysis</a></li>
    <li><a href="#risk-assessment">Risk Assessment</a></li>
    <li><a href="#security-profile">Security Profile</a></li>
    <li><a href="#integration-points">Integration Points</a></li>
    <li><a href="#constitutional-alignment">Constitutional Alignment</a></li>
    -->
  </ol>
  <div class="toc-progress">
    Progress: <strong><span id="progress-count">0</span> / 9</strong> sections authored
    <span class="toc-progress-dots" aria-hidden="true">
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
      <span class="dot"></span><span class="dot"></span><span class="dot"></span>
    </span>
  </div>
</aside>

<main class="main" id="main" tabindex="-1">

<header class="spec-meta">
  <span class="phase-tag">Phase: plan</span>
  <h1><<PHASE_TITLE>></h1>
  <p><strong>Source</strong>: <code><<SOURCE_MD>></code></p>
  <p><strong>Phase</strong>: plan &middot; <strong>Generated by</strong>: <code>deviate html plan</code></p>
  <p class="starter-note">
    <strong>Starter scaffold</strong> &mdash; author the body from <code><<SOURCE_MD>></code>.
    This is the per-issue <em>plan.md</em>; one per <code>ISS-NNN-NN</code> bucket.
    Tag every AC with <code class="ac">AC-PLAN-NNN</code> for traceability to JUDGE.
    <span class="chip chip-todo">9 sections to fill</span>
  </p>
</header>

<section id="plan-summary">
  <h2>Plan Summary <span class="chip chip-draft">elevator</span></h2>
  <dl class="kv">
    <!--
    <dt>Issue</dt>
    <dd><strong>ISS-NNN-NN</strong> &mdash; <em>one-line title</em></dd>
    <dt>Implementation Strategy</dt>
    <dd><em>architectural shape of the change (top-down / bottom-up / vertical-slice / hotfix)</em></dd>
    <dt>Estimated Complexity</dt>
    <dd><em>Low / Medium / High</em> &mdash; 1&ndash;3 sentences justifying the rating</dd>
    <dt>Estimated Effort</dt>
    <dd><em>range in engineer-hours, plus confidence</em></dd>
    <dt>Flow Anchors</dt>
    <dd><code class="flow-ref">FLOW-XX</code> &middot; <code class="flow-ref">FLOW-YY</code></dd>
    <dt>Architecture Anchor</dt>
    <dd><code>specs/_product/architecture.md</code> &mdash; <em>name the components touched</em></dd>
    -->
    <dt><em>Issue</em></dt><dd><em>TODO</em></dd>
    <dt><em>Implementation Strategy</em></dt><dd><em>TODO</em></dd>
    <dt><em>Estimated Complexity</em></dt><dd><em>TODO</em></dd>
    <dt><em>Estimated Effort</em></dt><dd><em>TODO</em></dd>
    <dt><em>Flow Anchors</em></dt><dd><em>TODO</em></dd>
    <dt><em>Architecture Anchor</em></dt><dd><em>TODO</em></dd>
  </dl>
</section>

<section id="acceptance-contract">
  <h2>Acceptance Contract <span class="chip chip-todo">AC-PLAN-NNN</span></h2>
  <div class="callout callout-note">
    <span class="callout-title">Token format</span>
    Each AC MUST carry a stable <code class="ac">AC-PLAN-NNN</code> tag
    (where <strong>NNN</strong> is a 3-digit zero-padded sequence). The
    <code>deviate judge</code> phase matches these tokens against <code>spec.md</code>
    invariants; a missing token blocks Gate 3.
  </div>

  <!--
    AC COMPONENT &mdash; duplicate per scenario.

    <article id="ac-plan-001" class="ac-card">
      <span class="chip chip-ac">AC-PLAN-001</span>
      <h3>Scenario: Clean Repository Initialization</h3>
      <div class="gherkin">
        <p><span class="gherkin-keyword given">Given</span> A repository root with no <code>.deviate/</code> directory</p>
        <p><span class="gherkin-keyword when">When</span> The user executes <code>deviate init</code></p>
        <p><span class="gherkin-keyword then">Then</span> <code>.deviate/config.toml</code> and <code>.deviate/</code> are created with a valid TOML structure matching <code>DeviateConfig</code>.</p>
      </div>
      <dl class="kv">
        <dt>Test path</dt><dd><code>tests/test_init.py::test_init_scaffolds_clean_repo</code></dd>
        <dt>FR trace</dt><dd><code class="fr">FR-001-INIT</code></dd>
        <dt>Flow trace</dt><dd><code class="flow-ref">FLOW-XX</code></dd>
      </dl>
    </article>
  -->

  <div class="component-block" aria-label="[TODO] Acceptance Contract entries">
    <span class="component-label">[TODO] Acceptance Contract</span>
    One <code>&lt;article id="ac-plan-NNN" class="ac-card"&gt;</code> per scenario.
    Each AC: <strong>tag</strong> + <strong>title</strong> + <strong>Gherkin body</strong> +
    <strong>test path</strong> + <strong>traces</strong>.
  </div>
</section>

<section id="workstation-mapping">
  <h2>Workstation Mapping</h2>
  <div class="callout callout-note">
    <span class="callout-title">Per-file contract</span>
    One row per file the change touches. Three required fields:
    <em>Current State</em> (what the file holds today, with line references),
    <em>Changes Required</em> (concrete diff level), <em>Integration Surface</em>
    (symbols exported, callers that depend on them).
  </div>

  <ul class="workstation-list">
    <!--
    WORKSTATION COMPONENT &mdash; duplicate per file.

    <li id="workstation-state-ledger">
      <h3><code>src/deviate/state/ledger.py</code> <span class="chip chip-draft">primary target</span></h3>
      <dl class="kv">
        <dt>Current State</dt>
        <dd>573 lines &mdash; contains <code>IssueRecord</code> at L25-37, <code>AdhocRecord</code> at L335-343, <code>_append_with_compound_key</code> at L115-142. Imports <code>Pydantic BaseModel</code>, <code>Field</code>, <code>field_validator</code>, <code>Literal</code>, <code>datetime</code>, <code>fcntl</code> (optional).</dd>
        <dt>Changes Required</dt>
        <dd>Add <code>FlowRecord</code>, <code>FlowEvent</code>, <code>FlowCoverage</code> Pydantic models after <code>AdhocRecord</code> (L343). Add <code>append_flow_record</code>, <code>append_flow_event</code>, <code>load_flow_coverage</code> helpers at end of module.</dd>
        <dt>Integration Surface</dt>
        <dd>Module-level exports &mdash; consumers import from <code>deviate.state.ledger</code>.</dd>
      </dl>
    </li>
    -->

    <li><em>TODO &mdash; one block per file with current state, changes required, integration surface</em></li>
  </ul>
</section>

<section id="implementation-strategy">
  <h2>Implementation Strategy</h2>
  <div class="callout callout-note">
    <span class="callout-title">Steps, not prose</span>
    Numbered phases. Each phase lists <em>files touched</em> and a single
    <em>verification command</em> the agent runs before declaring the phase done.
  </div>

  <ol class="strategy-steps">
    <!--
    STRATEGY COMPONENT &mdash; one entry per implementation phase.

    <li id="phase-1">
      <h3>Phase 1 &mdash; Pydantic models + append helpers</h3>
      <dl class="kv">
        <dt>Files</dt><dd><code>src/deviate/state/ledger.py</code></dd>
        <dt>Approach</dt><dd>Append three Pydantic models and three helpers at the end of the module. Reuse the canonical <code>_FLOW_REF_PATTERN</code> (referenced, not redefined).</dd>
        <dt>Verification</dt><dd><code>mise run test tests/test_state/test_ledger.py -v</code> &mdash; all four new tests pass.</dd>
      </dl>
    </li>
    -->

    <li><em>TODO &mdash; one numbered entry per implementation phase</em></li>
  </ol>
</section>

<section id="data-flow-analysis">
  <h2>Data Flow Analysis</h2>
  <p>Trace the data layer-to-layer. A reader who skips the rest of the plan can
     understand the change from this section alone.</p>

  <ol class="dataflow-steps">
    <!--
    <li><strong>Input layer</strong>: <code>specs/_product/flows/index.md</code> (markdown table) + <code>specs/issues.jsonl::IssueRecord.flow_refs</code>.</li>
    <li><strong>Seed step</strong>: parse index.md &rarr; <code>FlowRecord</code> list &rarr; idempotent append.</li>
    <li><strong>Derive step</strong>: replay <code>FlowEvent</code> history &rarr; status + drift_flag per flow.</li>
    <li><strong>Render step</strong>: Rich <code>Table</code> with six columns; drift rows in yellow.</li>
    <li><strong>Storage</strong>: <code>specs/_product/flows.jsonl</code> append-only &mdash; canonical source.</li>
    -->
    <li><em>TODO</em></li>
  </ol>
</section>

<section id="risk-assessment">
  <h2>Risk Assessment <span class="chip chip-draft">impact &times; likelihood</span></h2>

  <table>
    <thead>
      <tr><th>Risk</th><th>Impact</th><th>Likelihood</th><th>Mitigation</th></tr>
    </thead>
    <tbody>
      <!--
      RISK COMPONENT &mdash; duplicate per risk. Use severity chips.

      <tr>
        <td><strong>Circular import between <code>state.ledger</code> and <code>cli.adhoc</code></strong></td>
        <td><span class="risk-medium">Medium</span></td>
        <td><span class="risk-medium">Medium</span></td>
        <td>Define the regex as a module-level constant with a canonical-source comment.</td>
      </tr>
      <tr>
        <td><strong>Drift-flag taxonomy additions break JSONL re-parse</strong></td>
        <td><span class="risk-high">High</span></td>
        <td><span class="risk-low">Low</span></td>
        <td>Literal enumerates only the seven values this slice introduces; new flags require a constitution bump.</td>
      </tr>
      -->

      <tr><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

<section id="security-profile">
  <h2>Security Profile <span class="chip chip-todo">threat model</span></h2>
  <div class="callout callout-warn">
    <span class="callout-title">Risk surface checklist</span>
    Every plan touches at least one surface. Mark <em>none</em> explicitly when
    a surface is not affected &mdash; silent omission is a fail.
  </div>

  <ul class="risk-surfaces">
    <!--
    SECURITY COMPONENT &mdash; duplicate per surface.

    <li id="risk-surface-auth">
      <strong>Auth</strong> &mdash; <span class="chip chip-ready">not affected</span>
      No new authentication boundary; existing <code>FR-005-SESSION</code> scope is unchanged.
    </li>
    <li id="risk-surface-secrets">
      <strong>Secrets</strong> &mdash; <span class="chip chip-ready">not affected</span>
      No new secret material; no new env var readers.
    </li>
    <li id="risk-surface-pii">
      <strong>PII</strong> &mdash; <span class="chip chip-ready">not affected</span>
      No PII ingested, derived, or persisted.
    </li>
    <li id="risk-surface-deserialization">
      <strong>Deserialization</strong> &mdash; <span class="chip chip-todo">in scope</span>
      Pydantic <code>FlowRecord.flow_id</code> field validator carries a strict regex; malformed values
      raise <code>ValidationError</code> <em>before</em> the ledger write (per constitution §3).
      Negative test: <code>test_flow_record_rejects_lowercase_id</code>.
    </li>
    <li id="risk-surface-file-paths">
      <strong>File paths</strong> &mdash; <span class="chip chip-todo">in scope</span>
      <code>specs/_product/flows.jsonl</code> written via <code>_append_record</code> which calls
      <code>Path.parent.mkdir(parents=True, exist_ok=True)</code>. Negative test:
      <code>test_append_flow_record_creates_parent_dir</code>.
    </li>
    -->
    <li><em>TODO &mdash; mark &ldquo;not affected&rdquo; or &ldquo;in scope&rdquo; per surface</em></li>
  </ul>
</section>

<section id="integration-points">
  <h2>Integration Points</h2>
  <p>Cross-module touch points. One bullet per boundary crossed.</p>
  <ul>
    <!--
    INTEGRATION COMPONENT &mdash; one entry per module crossing.

    <li><strong><code>src/deviate/state/ledger.py</code> &harr; <code>src/deviate/cli/macro.py</code></strong>: <code>explore_post</code> imports the new <code>FlowRecord</code> / <code>FlowEvent</code> / <code>append_flow_record</code> / <code>append_flow_event</code> / <code>load_flow_coverage</code> symbols.</li>
    <li><strong><code>src/deviate/cli/macro.py</code> &harr; <code>specs/_product/flows/index.md</code></strong>: new <code>_parse_flows_index</code> consumes the markdown table format with <code>|</code>-delimited columns; header re-order breaks the seed step &mdash; log <code>FLOWS_INDEX_PARSE_FAILED</code> and halt.</li>
    -->
    <li><em>TODO</em></li>
  </ul>
</section>

<section id="constitutional-alignment">
  <h2>Constitutional Alignment <span class="chip chip-draft">§1&ndash;§6</span></h2>
  <p>One row per constitutional section affected. <strong>Empty rows are
     a fail</strong> &mdash; explicit &ldquo;no change&rdquo; belongs in the cell.</p>

  <table>
    <thead>
      <tr><th>Section</th><th>Alignment</th></tr>
    </thead>
    <tbody>
      <!--
      CONSTITUTIONAL ALIGNMENT COMPONENT

      <tr><td>§1 Append-Only Ledger</td><td>Aligned &mdash; <code>flows.jsonl</code> is the third append-only ledger, parallel to <code>issues.jsonl</code> and <code>tasks.jsonl</code>; the <code>FlowCoverage</code> derivation is emit-only and never persisted.</td></tr>
      <tr><td>§2 Database</td><td>Aligned &mdash; <code>flows.jsonl</code> enumerated under §2 (db row).</td></tr>
      <tr><td>§3 Validation</td><td>Aligned &mdash; Pydantic <code>FlowRecord.flow_id</code> carries strict regex validation; malformed rows raise <code>ValidationError</code> before write.</td></tr>
      <tr><td>§4 Commits</td><td>Aligned &mdash; <code>flows.jsonl</code> committed alongside the rest of the slice; CHANGELOG bullet appended under <code>[Unreleased] → Added</code>.</td></tr>
      <tr><td>§5 Definition of Done</td><td>Aligned &mdash; tests added at the unit + integration tier; <code>mise run check</code> exits 0 with new code in place.</td></tr>
      <tr><td>§6 Version History</td><td>Aligned &mdash; constitution pre-bumped to v0.7.0 by a prior commit on this branch (verified at <code>specs/constitution.md:96</code>); no further bump required by this slice.</td></tr>
      -->

      <tr><td><em>TODO &mdash; one row per §N</em></td><td>&mdash;</td></tr>
    </tbody>
  </table>
</section>

</main>
</div>
</body>
</html>
